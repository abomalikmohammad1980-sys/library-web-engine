import {json} from '../_account-contract.js'
export async function onRequestGet(context){if(!context.env?.VISITORS_DB)return json({error:'central_overrides_unavailable'},503);const result=await context.env.VISITORS_DB.prepare("SELECT book_id AS bookId,title,author,category,visibility,revision,CASE WHEN logically_deleted_at IS NULL THEN 0 ELSE 1 END AS logicallyDeleted,updated_at AS updatedAt FROM central_book_overrides").all();const overrides=(result.results??[]).map(row=>({bookId:row.bookId,...(row.title?{title:row.title}:{}),...(row.author?{author:row.author}:{}),category:row.category??null,visibility:row.visibility,revision:Number(row.revision??0),logicallyDeleted:Boolean(row.logicallyDeleted),updatedAt:row.updatedAt}));return json({schemaVersion:1,overrides},200,{'cache-control':'public, max-age=30, stale-while-revalidate=120'})}
export const onRequest=()=>json({error:'method_not_allowed'},405,{allow:'GET'})
