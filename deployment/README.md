# Production source checkpoint — batch33

Published 2026-09-17 to https://khzanah.com and https://a5ff7588.khezana.pages.dev.
The staged app/packages sources are the frozen source used for this deployment;
newer unfinished working-tree search changes were not activated.

`cloudflare/` preserves the deployed server functions, migrations, configuration
and deployment tooling from the separate local `alpha-publish` repository.
For a fresh checkout, copy its contents to `alpha-publish/` before using the
existing build tooling. Library data and frozen build artifacts are maintained
separately; this is not a self-contained copy of the book corpus.

See `batch33-deployment.json` for checked URLs, immutable fingerprints, rollback
reference and remaining acceptance caveats. Do not interpret publication as
closure of field-separated search, BOK cloud activation or every edition link.
