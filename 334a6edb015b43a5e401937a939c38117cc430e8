import type{SyncAccountUsage}from"@library/source-sync";import type{AuthenticatedPrincipal}from"./contracts.js";export interface AccountUsageStore{get(userId:string):Promise<SyncAccountUsage|null>}export class AccountUsageHttpRouter{constructor(private readonly store:AccountUsageStore,private readonly auth:{authenticate(r:Request):Promise<AuthenticatedPrincipal>}){}async handle(r:Request){try{if(r.method!=="GET")return Response.json({error:{code:"method_not_allowed"}},{status:405});const p=await this.auth.authenticate(r),usage=await this.store.get(p.userId);return Response.json(usage??{error:{code:"usage_not_found"}},{status:usage?200:404})}catch{return Response.json({error:{code:"unauthenticated"}},{status:401})}}}
