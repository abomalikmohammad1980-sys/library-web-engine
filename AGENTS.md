# دستور الوكلاء — Library Web Engine

أنت وكيل ضمن أسطول ينفذ هذا المشروع تحت إشراف منفّذ بشري واحد. **اقرأ هذا الملف كاملًا قبل أي مهمة.** كل ما تحتاج معرفته موجود في الريبو — لا تفترض سياقًا من محادثات سابقة.

## ما هذا المشروع

محرك عرض ويب للكتب العربية (docx بتنسيق الشاملة) هدفه **مطابقة هندسية كاملة مع Microsoft Word** — خاصة النص العربي (كسر الأسطر، ارتفاع السطر، الكشيدة). اقرأ حسب حاجة مهمتك:

- `docs/architecture.md` — الخطة المعمارية (القرارات الكبرى ولماذا)
- `docs/prd.md` — ماذا يفعل المستخدم وما ليس في النطاق
- `docs/playbook.md` — نموذج التشغيل (طبقات المراجعة، دورة المهمة)
- `docs/word-behavior-spec/` — **قواعد سلوك Word المكتشفة — اقرأ ما يمس دلالتك قبل أن تكتب سطرًا**
- `docs/adr/` — القرارات المعمارية الملزمة
- `reference/dart-engine/` — المحرك الأصلي (Dart) الذي تُترجم منه + ‏`AGENTS.md` القديم بدروسه الثلاثة والعشرين
- `reference/ooxml-spec/` — مستخلصات ECMA-376: المرجع الأول لدلالة أي عنصر XML

## خريطة الحزم وحدودها

| الحزمة | المسؤولية | طبقة الدمج |
|---|---|---|
| `packages/ooxml-model` | تحليل OOXML ← شجرة نموذج محلولة الأنماط (+`w:compat`) — تُترجم من `reference/dart-engine` | 🟠 |
| `packages/font-system` | جداول الخطوط، المقاييس بطريقة Word، مخارج الغليفات، كل أوجه العائلة | 🔴 |
| `packages/shaper` | غلاف HarfBuzz-WASM + كاش + خرائط العناقيد | 🟠 |
| `packages/bidi` | غلاف تنفيذ UAX#9 مرجعي + خصوصيات Word | 🟠 |
| `packages/layout` | **قلب المشروع**: كاسر الأسطر، ارتفاع السطر، التسويغ الغليفي/الكشيدة، الفقرة/الجدول/الصفحة | 🔴 |
| `packages/scene` | شجرة المشهد + تسلسل «حزمة الكتاب» | 🟠 |
| `packages/paint` | الرسم Canvas + كاش المسارات + مولد PDF | 🟠 |
| `packages/interact` | إصابة/تحديد/حافظة/تظليل | 🟠 |
| `packages/a11y` | طبقة DOM المرآة | 🟠 |
| `packages/search` | sqlite-wasm + FTS5 + المحلل الصرفي | 🟠 |
| `packages/oracle-client` | المقارن الرقمي ضد ملفات الحقيقة | 🟢 |
| `tools/*` | word-oracle, census, prep, corpus | 🟢 |
| `app/` | قشرة الواجهة | 🟢 |

**قاعدة الحدود: لا تعدّل خارج حزمة مهمتك.** ما يعبر الحدود يمر عبر عقد `SPEC.md` الخاص بالحزمة.

## القواعد الصارمة (مخالفتها = رفض الـ PR)

1. **`reference/` قراءة فقط** — لا يُعدَّل حرف فيه.
2. **لا حساب float في `layout`** — الحساب الداخلي على شبكة twips (أعداد صحيحة، 1/20pt) وEMU للأشكال. float فقط عند التحجيم للرسم. (ADR-0004)
3. **الكشيدة والتسويغ غليفيًا** — يُمنع منعًا باتًا إدراج محارف تطويل (U+0640) في النص أو أي تعديل على نص المستند لأغراض العرض. (ADR-0005)
4. **لا تعديل على `SPEC.md` مجمد** إلا بـ ADR جديد يعتمده المنفّذ.
5. **كل قاعدة سلوك Word تكتشفها تُدوَّن في `docs/word-behavior-spec/` قبل إغلاق المهمة** — بصيغة: الحالة المؤكدة، الدليل (XML + رقم مُحكِّم)، القاعدة، اختبارها. معرفتك تموت بانتهاء جلستك؛ المدوَّن فقط يبقى.
6. **معيار القبول آلي دائمًا** — «يبدو صحيحًا» ليس دليلًا. الدليل: اختبار يمر، رقم مُحكِّم لا ينحدر، مقارنة تفاضلية تتطابق.
7. **الترجمة من Dart تحافظ على أسماء المفاهيم** (الحقول، الملفات) ليبقى التدقيق التفاضلي ممكنًا سطرًا بسطر.
8. **ممنوع إصلاح أي خلل عرض بترقيع بصري** (إزاحة بكسلات، استثناء لكتاب بعينه). ابدأ من XML والمواصفة — نفس منهجية `reference/dart-engine/AGENTS.md` التي أنجحت المشروع الأصلي.
9. **قاعدة التصعيد:** إن لم تحرز تقدمًا بعد 3 محاولات إصلاح متمايزة، توقف واكتب «تقرير عجز» في المهمة: ما جربت، ما استبعدت، أين تشك. لا تدُر دورانًا أعمى.

## أوامر التشغيل

```bash
pnpm install              # مرة واحدة
pnpm build                # بناء كل الحزم
pnpm test                 # كل الاختبارات
pnpm -F <package> test    # اختبارات حزمة واحدة
# المُحكِّم على عينة (بعد بنائه في المرحلة 0):
pnpm oracle:check         # مقارنة مخرجات layout ضد corpus/ground-truth
```

## دورة عملك في المهمة

1. اقرأ المهمة (Issue) كاملة + هذا الملف + `SPEC.md` لحزمتك + ما يمسك من `word-behavior-spec`.
2. اعمل على فرع باسم `task/<رقم>-<وصف-قصير>` (في worktree مستقل إن كنت متوازيًا مع غيرك).
3. اكتب الاختبار الذي يجسد معيار القبول أولًا، ثم نفّذ حتى يخضرّ، وكرر ضد المُحكِّم إن كانت المهمة تخطيطية.
4. افتح PR بقالب الريبو: حدد طبقة الخطورة، أرفق الدليل (أرقام قبل/بعد).
5. عالج ملاحظات المراجع الخصومي، ثم انتظر دمج المنفّذ (🔴/🟠) أو الدمج الآلي (🟢).

## معلومة تحكم أخيرة

المقياس الأوحد للتقدم هو **نسبة الصفحات المتطابقة مع Word** — لا حجم الكود ولا عدد المهام. إن وجدت نفسك تحسّن شيئًا لا يحرك هذا الرقم ولا يخدم مهمة مفتوحة، توقف واسأل.

## مصادر وكيل البحث — الترتيب والانضباط

عند استعصاء فئة فشل على القياس المباشر، يُطلق وكيل بحث بسؤال محدد، بهذا الترتيب:

1. **‏[MS-OI29500]** — ملاحظات التنفيذ الرسمية من مايكروسوفت (انحرافات Word عن ECMA-376): نظيف قانونيًا وأولى من أي كود.
2. **‏LibreOffice Writer** ‏(MPL): عقدان من كود توافق Word (رايات compat، كسر أسطر Word97).
3. **‏OnlyOffice sdkjs** ‏(⚠️ AGPL): الأقرب معماريًا — **Clean Room حصرًا**: وكيل البحث يستخرج وصفًا سلوكيًا إلى المواصفة الحية، ووكيل التنفيذ لا يرى الكود أبدًا.
4. وثائق **Uniscribe/DirectWrite** لأصناف التسويغ (أساس ADR-0005).

**القاعدة:** كل ما يُستخرج يدخل **فرضيةً تُقاس بالمُحكِّم** — لا حقيقة تُنسخ. وكل فرضية مصدرها AGPL تمر عبر وصف سلوكي وسيط موثق.
