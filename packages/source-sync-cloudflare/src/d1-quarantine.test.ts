import{describe,expect,it}from"vitest";import{D1QuarantineResolutionStore}from"./d1-quarantine.js";import type{D1DatabaseBinding}from"./bindings.js";
describe("D1 quarantine",()=>{it("requires an active lease and writes metadata-only audit",async()=>{const sql:string[]=[],db:D1DatabaseBinding={prepare(s){sql.push(s);return{bind(){return this},first:async()=>null,all:async()=>({success:true,results:[]}),run:async()=>({success:true,meta:{changes:1}})}}};const store=new D1QuarantineResolutionStore(db);expect(await store.resolve({quarantineId:"q",resolutionId:"op",leaseId:"lease",decision:"allow",reason:"reviewed",expectedRecordVersion:2,adminId:"a"})).toBe(true);expect(sql[0]).toContain("resolution_lease_id=?7");expect(sql[0]).toContain("resolution_lease_until_ms>=?8");expect(sql.join(" ")).not.toMatch(/token|content|object_key/i)})});
