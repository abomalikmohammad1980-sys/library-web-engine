import{describe,expect,it}from"vitest";import{D1DeviceRegistryStore}from"./d1-device-registry.js";import type{D1DatabaseBinding}from"./bindings.js";
describe("D1 devices",()=>{it("scopes active checks and revocation to principal+device",async()=>{const sql:string[]=[],db:D1DatabaseBinding={prepare(s){sql.push(s);return{bind(){return this},first:async()=>({active:1}),all:async()=>({success:true,results:[]}),run:async()=>({success:true,meta:{changes:1}})}}},store=new D1DeviceRegistryStore(db);expect(await store.isActiveDevice("u","d")).toBe(true);expect(await store.revoke("u","d")).toBe(true);expect(sql[0]).toContain("principal_sub=?1 AND device_id=?2 AND revoked_at IS NULL");expect(sql[2]).toContain("principal_sub=?1 AND device_id=?2")})});
