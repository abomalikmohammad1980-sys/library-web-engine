export interface QuranTextDelta{schemaVersion:1;baseChecksumSha256:string;nextChecksumSha256:string;added:string[];changed:string[];removed:string[]}
export function computeQuranTextDelta(base:readonly{ayahId:string;text:string}[],next:readonly{ayahId:string;text:string}[],checksums:{base:string;next:string}):QuranTextDelta{if(!/^[a-f0-9]{64}$/.test(checksums.base)||!/^[a-f0-9]{64}$/.test(checksums.next))throw Error("quran_delta_checksum_invalid");const left=index(base),right=index(next),added=[],changed=[],removed=[];for(const[id,text]of right)if(!left.has(id))added.push(id);else if(left.get(id)!==text)changed.push(id);for(const id of left.keys())if(!right.has(id))removed.push(id);const sort=(values:string[])=>values.sort((a,b)=>a.localeCompare(b,"en",{numeric:true}));return{schemaVersion:1,baseChecksumSha256:checksums.base,nextChecksumSha256:checksums.next,added:sort(added),changed:sort(changed),removed:sort(removed)}}
function index(records:readonly{ayahId:string;text:string}[]){const map=new Map<string,string>();for(const item of records){if(!/^\d{1,3}:\d{1,3}$/.test(item.ayahId)||!item.text||map.has(item.ayahId))throw Error("quran_delta_record_invalid");map.set(item.ayahId,item.text)}return map}
