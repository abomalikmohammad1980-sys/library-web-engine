{
  "name": "library-web-engine",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "oracle:check": "echo 'oracle: يُبنى في المرحلة 0 (tools/word-oracle + packages/oracle-client)' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
