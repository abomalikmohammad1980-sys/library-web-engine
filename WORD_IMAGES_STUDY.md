# دراسة تنفيذية شاملة لصور ورسومات Word في عارض الويب

التاريخ: 2026-08-06  
الحالة: مرحلة تنفيذ نشطة — لا تُعد الصور منجزة قبل إغلاق بوابات القبول أدناه.

## الهدف

عرض الصورة/الرسم في الويب بالمصدر والمقاس والقص والموضع والطبقة والالتفاف نفسها
التي يحددها ملف Word، سواء كانت في المتن أو جدول أو رأس أو تذييل أو مربع نص.

## مصفوفة المصادر

| الفئة | تمثيل OOXML | الحالة الحالية | العمل الملزم |
|---|---|---|---|
| صورة سطرية | `wp:inline/a:blip` | موجود | مراجعة baseline والمسافات والتأثيرات |
| صورة عائمة | `wp:anchor/a:blip` | موجود أساسيًا | استكمال جميع المراجع والمحاذاة والتداخل |
| VML قديم | `w:pict/v:shape/v:imagedata` | تحليل جزئي | المقاس، crop، rotation، z-index، العلاقات |
| توافق بديل | `mc:AlternateContent` | موجود جزئيًا | اختيار Choice المدعوم وإلا Fallback بلا تكرار |
| SVG حديث | `asvg:svgBlip` مع fallback | منفذ | يُفضّل SVG ثم fallback النقطي |
| EMF/WMF | media relationship | PNG/JPEG/DIB داخل EMF وDIB داخل WMF، وأوامر EMF/WMF الهندسية ونصوص EMF إلى SVG | باقية التدرجات والعمليات الرسومية المركبة النادرة |
| صورة داخل جزء | header/footer/footnote/textbox | علاقات الأجزاء موجودة | منع الرجوع الخاطئ إلى document.xml |
| مجموعة/SmartArt | `wpg` / `dgm:relIds` | أساسي | التحويلات، ترتيب الطبقات، النص داخل الأشكال |

## مصفوفة الخصائص

1. الهندسة: `extent`، المقاسات النسبية `sizeRelH/V`، الدوران، flipH/V، القفل ونسبة الأبعاد.
2. القص والملء: `srcRect`، stretch/tile، موضع الصورة، الشفافية، grayscale، duotone.
3. التموضع: page/margin/column/paragraph/character وداخل الجداول، align أو offset.
4. الطبقات: `behindDoc`، `relativeHeight`، ترتيب XML، `allowOverlap`.
5. الالتفاف: none، square، tight، through، topAndBottom، مسافات distL/R/T/B، مضلع wrapPolygon.
6. السياق: المتن، الجدول، الرأس، التذييل، الحاشية، مربع النص، المجموعة.
7. الدلالات: alt/title، hyperlink، hidden/print، first/even/default header.

## بوابات القبول العملية

- لا توجد علاقة صورة في أي جزء تُحل إلى `null` من دون سبب مسجل.
- عدد الرسومات المستخرجة = عدد الرسومات القابلة للعرض أو ذات fallback معلن.
- كل صورة لها صفحة وموضع وطبقة وأبعاد قابلة للمقارنة مع Word.
- عينات مستقلة لكل فئة: inline، anchor، VML، SVG، EMF/WMF، header/footer، table، crop/rotate/flip، وكل wrap.
- المقارنة البصرية تشمل أول وآخر نص حول الصورة حتى لا يؤدي الالتفاف إلى إسقاط نص.
- لا تُغلق المرحلة قبل تجربة رفع ملفات متنوعة من الواجهة نفسها.

## الحالة التنفيذية الحالية

- منفذ: `asvg:svgBlip` مع fallback، والقص والدوران والانعكاس.
- منفذ: `alphaModFix` وgrayscale والسطوع والتباين والظل والتوهج والحافة الناعمة وإطار الصورة.
- منفذ: `sizeRelH/V` و`allowOverlap` و`layoutInCell` والطبقات.
- منفذ: مضلع `wrapPolygon` لـTight/Through.
- منفذ: استخراج PNG/JPEG وDIB ‏32-bit من EMF وتحويله إلى BMP ‏24-bit، واستخراج DIB من WMF.
- منفذ: duotone بمرشح SVG لوني، وreflection بتدرج شفافيته ومسافته.
- منفذ: أوامر EMF المتجهية للأقلام والفُرش والخطوط والمستطيلات والبيضاوي والمضلعات، ومسارات Begin/End/Close/Fill/Stroke ومنحنيات Bézier، إلى SVG.
- منفذ أساسي: WMF المتجهي الخالص للأقلام والفُرش والخطوط والمستطيلات والبيضاوي والمضلعات إلى SVG.
- منفذ: نصوص EMF ‏Unicode/ANSI مع اللون والخط والحجم والوزن والميل إلى SVG.
- منفذ: `EMR_GRADIENTFILL` للمستطيلات الأفقية والرأسية، مع حفظ قنوات `TRIVERTEX` وحدود كل `GRADIENT_RECT` في SVG.
- منفذ: `META_EXTTEXTOUT` في WMF، بما فيه موضع النص ومصفوفة تقدم المحارف `dx`.
- باقٍ: مثلثات `GRADIENT_FILL_TRIANGLE`، والعمليات الرسومية المركبة النادرة، وتوسيع هندسات الأشكال المسبقة.
