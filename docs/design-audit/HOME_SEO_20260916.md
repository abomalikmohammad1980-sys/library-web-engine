# Homepage title and initial rendering — 2026-09-16

- Unified homepage title/H1: `الخزانة: المكتبة الإسلامية الذكية` (server, metadata model, client hero, static shell).
- Added a descriptive homepage summary and WebSite identity, with concise navigation labels instead of repeated full SEO titles.
- Server HTML now includes scoped critical styling. Public content remains visible and usable without JavaScript; no cloaking or display:none. The SPA still replaces the initial public page; this is not full hydration.
- `node --test tools/seo-server.test.mjs`: passed actual HTMLRewriter, titles, one H1, canonical, 404/private pages and sitemap counts.
- Metadata/invalidation unit tests: 5 passed. Broader search/selection run: 41 passed, 2 skipped; the previously documented normalizer-source fingerprint test still fails and was not changed.
- Typecheck and frozen build: passed in complete-search-batch29.mjs.
- Final preview: https://ec33bd2d.khezana.pages.dev ; HTTP acceptance `.artifacts/batch29/preview-http.json` passed (11,775 static sitemap URLs).
- Browser: verified homepage title and H1 after SPA mount. Separately inspected server-only initial HTML at localhost with scripts removed: styled heading, description, navigation and book cards; no raw unstyled list.
- Heading search: earlier preview experienced `heading_release_timeout`; retry did not provide a verified completed result during observation. Do not claim UI search acceptance or sub-second performance closed.
- Google title/rank updates depend on recrawl and Google's selection; no promise of immediate first-page ranking.
- Production publication must be recorded separately after live verification.
