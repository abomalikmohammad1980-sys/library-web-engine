# Account readiness incident and release77

Production76 /api/account/readiness returned503, configured Access but both runtime checks false. Native-session without cookies returned401 correctly. Read-only D1 schema inspection confirmed every account table and column exists; zero-row queries succeeded. A real one-row readiness-equivalent query failed with Cloudflare7500: daily free-tier row-read quota exhausted, resetting midnight UTC. This is an account-wide provider resource limit, not deleted accounts or removed Access settings. Full login cannot be claimed restored before quota renewal. No paid upgrade, alternative quota circumvention, test production account, or weakened authentication was introduced.

Readiness now validates schema without reading/joining user records, performs a bounded actual read to detect quota failures, suppresses repeated failure probes for30seconds, and reports explicit sanitized quota/schema/binding/temporary reasons. Client no longer labels temporary outages as never-configured and offers a manual retry. Server5xx on password submission is not falsely reported as a wrong password. Actual password/session/device/block/authority enforcement is unchanged.

Three server recovery tests and83 focused client tests passed; app typecheck passed. Earlier reader-information31tests and visual acceptance are included through dc69a89. Batch77 stages from verified production76 (all unchanged static data preserved), replacing the finalized client and only the reviewed readiness Function. Rollback target76. Deployment and live recovery outcome are recorded separately in batch77 receipts. The dominant historical quota consumer is not established by the readiness diagnosis; do not claim that this bounded check alone prevents future exhaustion.
