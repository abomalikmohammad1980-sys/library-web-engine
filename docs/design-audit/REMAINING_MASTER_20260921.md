# سجل الإغلاق المرجعي — 2026-09-21

هذا السجل مرجع الأعمال الحالية؛ السجلات الأقدم أدلة تاريخية لا تعيد فتح بند ثبت إغلاقه. التنفيذ بندًا بعد بند. لا تعني كتابة الإصلاح أنه منشور، ولا يعني نجاح عينة قبول المكتبة كاملة.

## قواعد الإغلاق

- الحالات: قيد العمل، ينتظر دوره، ينتظر المستخدم، متحقق محليًا، منشور ومتحقق، منتهٍ بلا نشر.
- لكل إغلاق: التغيير، دليل الاختبار، نطاقه، إيصال النشر عند الحاجة، وما لا يشمله الدليل.
- لا يعاد فتح المغلق إلا بدليل خلل جديد؛ يضاف حينها بند مرتبط بدل محو تاريخ الإنجاز.
- لا بيانات اختبار في الإنتاج، ولا تفعيل جزئي لفصل الحقول أو BOK.

## قائمة العمل بالترتيب

| الرقم | البند | الحالة | شرط الإغلاق |
|---|---|---|---|
| 01 | تشخيص استهلاك D1 الذي عطّل الحسابات وتقليله | قيد العمل | إثبات المستهلك بالقياس، إصلاح موجّه واختبارات أمان ثم نشر وتحقق؛ لا ادعاء ضمان عدم نفاد الحصة |
| 02 | قبول فصل حقول البحث | ينتظر دوره | تغطية كاملة ومطابقة النص والعنوان والحاشية، اختبار المرشح الكامل قبل تفعيل موحد |
| 03 | قبول BOK السحابي الكامل | ينتظر دوره | المكتبة الكاملة، القارئ والبحث والملاحظات، الاستبدال والتراجع في بيئة معزولة |
| 04 | دورة استيراد Word/PDF وبقية الصيغ والفهرسة الآلية | ينتظر دوره | رفع وتحويل وفهرسة وسحب لكل صيغة، وإثبات المشغّل الدوري؛ OCR مؤجل بطلب المستخدم |
| 05 | الطبعات المرتبطة | ينتظر دوره | رفع/تحميل الأصل وسحب الطبعة وواجهة المتصفح؛ اختبارات handlers وحدها غير كافية |
| 06 | الحسابات والإدارة والمزامنة | ينتظر دوره | دخول فعلي مأذون، صلاحيات وحذف/تعديل جماعي وتزامن جلسات، بلا حساب تجريبي إنتاجي |
| 07 | قبول المحتوى والواجهات المتبقي | ينتظر دوره | روابط التفسير للآيات، النسخ العثماني، المؤلفون والأماكن والصفات، الروسية والتوصيات حسب أدلتها |
| 08 | فرز إخفاقات حزمة الاختبارات وسجل الترحيلات | ينتظر دوره | فصل اختبار قديم عن خلل حقيقي؛ إصلاح السبب وتسجيل نتيجة شاملة محددة النطاق |
| 09 | الدرر وأحكام المحققين الأربعة | ينتظر المستخدم | وصول مأذون أو تصدير رسمي ثم تحقق؛ لا تجاوز لرفض 403 |

سرعة أول فتح للجوال وأول بحث ليست مقفلة بالكامل؛ تجارب الأداء متوقفة بطلب المستخدم، ولا تُعاد تلقائيًا تحت بند آخر.

## منجزات محفوظة لا تكرر

- **منشور ومتحقق:** batch77 على khzanah.com؛ المرجع `alpha-publish/ops/current-production.json` وإيصال `batch-20260921-77-deployment.json`؛ 20,000 ملف و145 بصمة، rollback76. يضم إجراءات معلومات الكتاب والأصول والطبعات المطوية وتشخيص جاهزية الحسابات.
- **تعافٍ مثبت لا يساوي دخولًا كاملًا:** جاهزية الحسابات HTTP200 بعد تجدد الحصة وواجهة الدخول سليمة؛ سجل `ACCOUNT_QUOTA_RECOVERY_20260921.md`. أصل استهلاك الحصة ما زال بند01.
- **منتهٍ بلا نشر:** تنظيف أول 125GiB ثم نحو130GiB إضافية مع دمج النسخ الفيزيائية المتطابقة بروابط صلبة وحفظ المسارات؛ تقريرا `D:/alkhizana/docs/CLEANUP_20260921.md` و`CLEANUP_VERIFIED_DUPLICATES_20260921.md`. لا تحذف أرشيفًا بسبب حجمه المنطقي وحده ولا تعدّل الأرشيفات المشتركة في مكانها.
- **مرحلة مكتملة فقط:** نقل المصدر8595 وboundary متحققان؛ لا تكرار نقل. غير مفعلين ولا يغلقان02 أو03. راجع `FIELD_BOK_CONTINUATION_20260918.md`.
- **منشور:** استعادة35 تفسيرًا والأسماء المختصرة؛ `TAFSIR_RESTORATION_20260918.md`. لا يثبت ذلك اكتمال كل روابط الآيات.

## سجل العمل

### 2026-09-21 — إضافة عاجلة: فتح الكتب وتفاعل بطاقة المعلومات

طلب المستخدم إصلاح بطء الكتاب2146 (`https://khzanah.com/books/2146`) ثم وضح أن تفاعل بطاقة المعلومات بطيء جدًا. قياس HTTP فقط أعطى200 وزمن0.87ثانية؛ **ليس قياسًا لظهور النص أو لاستجابة المتصفح**.

- نُفّذ محليًا تحميل بيانات مسار الشاملة العامة بالتوازي مع فحص الظهور وقراءة النسخة المحلية؛ لم يُنقل تحميل متن الكتاب قبل فحص الظهور، ولم يتغير مسار عدم الاتصال.
- نُفّذ محليًا تحميل أصل Word وخريطة صفحاته وPDF المرافق بالتوازي بدل التتابع؛ جميع ضوابط البصمة وصحة الخريطة باقية.
- نجحت28 حالة موجهة في5 ملفات اختبار لمسارات الاستيراد وفتح الشاملة، ونجح فحص TypeScript.
- نُفّذ طلب التنسيق محليًا: الإجراءات تحت الغلاف مباشرة، زر46px ورمز24px؛ إخفاء نص عناوين الصيغة/التصنيف/سنة النشر/عدد الصفحات مع title وaria-label وتركيز لوحة المفاتيح، والإبقاء على قيمة مثل «٨٤ صفحة».
- **غير مغلق وغير منشور:** التحقق المرئي وقياس تجمد التفاعل للكتاب2146 لم يكتمل؛ نجاح الاختبارات لا يثبت زوال المشكلة كلها. يلزم مرشح جديد لا تعديل batch77 المجمد.

تصحيح تشخيص الحسابات: فحص الموقع بعد خطأ7500 أعطى جاهزية200 وready=true الساعة05:47:47UTC؛ لا يُستنتج تعطل الحسابات من فشل طلب التشخيص وحده ولا يُعاد إبلاغ المستخدم بعطل غير مثبت.

### 2026-09-21 — بدء01

قرئت تعليمات المشروع وإيصال الإنتاج وتشخيص الحصة. فحص المصدر يبيّن أن قوائم SEO تعد الكتب الديناميكية وتعيد الاستعلام للتحقق من ثبات الظهور. هذه فرضية استهلاك لا إثبات أنها السبب الأكبر؛ لا إزالة لفحص الخصوصية أو السحب بغرض تقليل الاستهلاك. الخطوة التالية قراءة إحصاءات D1 حسب الصفوف المقروءة قبل اختيار الإصلاح.

**دليل جديد:** نجحت قراءة `wrangler d1 insights khezana-visitors --time-period 1d --sort-by reads --sort-type sum --limit 8 --json` على حساب الإنتاج الصحيح. أعلى استعلام: فحص `central_book_overrides WHERE book_id IN (SELECT value FROM json_each(?1))`، بعدد23,388 تنفيذًا و7,847,828 صفًا مقروءًا (متوسط335). التالي55,590 فقط. المصدر `_seo-listings.js:46`. هذا يحدد المستهلك الأكبر في نافذة القياس، لا يثبت وحده عدد الزيارات البشرية أو كل أسباب الحصة. نختبر استبدال تعداد JSON باستخراج مفاتيحه دون إسقاط فحص الإخفاء الفوري. المحاولة الأولى اختارت حسابًا قديمًا وفشلت10000؛ نجحت بعد تعيين حساب الإنتاج، ولا حاجة لتجديد الدخول.

**إصلاح محلي واختبار:** استبدل `json_each` بمسارات `json_extract` ثابتة مولدة من طول قائمة معرّفات متحقق منها (حد300 اسم بديل)، مع إبقاء معامل مربوط واحد وفحص حديث بلا تخزين إذن الظهور. عُدّل المصدر والمرآة فقط، لا بناء77 المجمد. نجحت5 اختبارات عبر `node --test tools/seo-listing-quota.test.mjs tools/seo-listings.test.mjs`؛ اختبار SQLite حقيقي يطابق نتائج الاستعلام القديم ويتحقق من بحث المفتاح الأساسي وعدم مسح جدول افتراضي ومن سريان السحب بين طلبين.

**عائق قياس حي جديد:** المقارنة المحدودة للقراءة فقط على قاعدة الإنتاج أعادت7500 مجددًا: نفاد الحصة اليومية. لا أثر قياس D1 مثبت للإصلاح بعد، ولا نشر جديد ولا ترقية مدفوعة. يبقى01 مفتوحًا بحالة «إصلاح محلي مختبر؛ القياس السحابي والنشر معلقان». لا تكرر استعلامات الإنتاج ما دامت الحصة منتهية. يمكن متابعة إعداد حزمة واختبارات معزولة محليًا، ثم قياس الانخفاض عند تجدد الحصة والتحقق من النشر.
