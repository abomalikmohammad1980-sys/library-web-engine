# الترصيف الرأسي — المواصفة الحية (بدأت 2026-07-13)

الهدف: مطابقة مواضع baselines كما يحسبها Word — الطريق إلى 100% يمر بالبعدين معًا.
العدّة: `tools/word-oracle/vertical_v1.mjs` — يقارن فروق baselines المتتالية داخل
الفقرة (نفس الصفحة) من الحقيقة ضد التنبؤ. حدود الفقرات وفواصل الصفحات مرحلة تالية.

## القاعدة الرأسية 1: خطوة السطر المفردة = مقاييس hhea

`singlePitch = (hhea.ascent − hhea.descent + hhea.lineGap) / upem × em`

تحقق يدويًا على ثلاثة خطوط قبل أي معايرة:
- ‏adwa ‏1.8916em ×300 = ‏567.5 → الحقيقة **568** ✓
- ‏trado ‏1.4946em ×400 = ‏597.8 → الحقيقة **598** ✓ (dawra)
- ‏trado ‏×320×1.15 = ‏**550.0** → الحقيقة 548–552 ✓ (jalsa)
- ‏jazeera ‏1.6900em ×320×(278/240) = ‏626.4 → الحقيقة 626–630 ✓ (ahadith)

(البدائل المرفوضة بالقياس نفسه: ‏OS/2 winAscent/Descent يطابق hhea في trado/adwa
لكن jazeera يفترقان — والحقيقة اختارت hhea: ‏1.690 لا 1.348.)

## القاعدة الرأسية 2: ‏w:spacing — المضاعف والحدود

- ‏lineRule=auto: ‏pitch = singlePitch × line/240.
- ‏exact: ‏pitch = line (twips). ‏atLeast: ‏max(single, line) — لم يظهر بعد في العينة.
- حدود الفقرات: ‏Δ = pitch + after(السابقة) + before(اللاحقة) — تحقق عرضيًا
  (ahadith: ‏626+160=786 ≈ العنقود 784–788 ✓؛ ‏jalsa: ‏550+200=750 ✓) ويقاس
  رسميًا في v2.

## القاعدة الرأسية 3: وراثة w:spacing — سمّية، وعبر نمط الفقرة الافتراضي

اكتُشفت بالتكذيب المتقاطع (كتابان بسلوكين «متناقضين»):

- **الوراثة سمّية لا عنصرية**: ‏after مباشر يتعايش مع line من النمط/docDefaults
  (تجربة العنصرية: ‏muqtarah ‏93.75%←18.75% — مكذوبة).
- **الفقرات بلا pStyle ترث نمط الفقرة الافتراضي (Normal، ‏w:default="1") قبل
  docDefaults**: ‏masjid: ‏Normal ‏line=240 يلغي docDefaults ‏line=276 —
  إضافة الطبقة رفعته 25%←78.33% دون مساس muqtarah (نمطه الافتراضي بلا spacing
  فيسقط لـdocDefaults ‏259 ✓).
- أثر جانبي مقيس على الأفقي: وراثة rFonts/sz من Normal **ضاعفت تغطية dawra**
  ‏(116←235 سطرًا مؤهلًا) عند 96.60% — توسع صادق لا انحدار.

## خط الأساس الرأسي v1 ‏(TOL=±2 twips، أزواج داخل الفقرة/الصفحة)

| الكتاب | النتيجة | ملاحظات أول تشغيلة |
|---|---|---|
| muqtarah | **93.75%** | البقية ±5 (تقريب مزدوج) |
| masjid | **78.33%** | البقية ±3 |
| dawra | 68.24% | ‏+2×كثير (تقريب pred مطلوب) |
| tadris | 69.54% | ‏−2 عنقود مماثل |
| ahadith | 67.92% | ‏+4 عنقود |
| jalsa27 | 65.03% | ‏−2×206 (شبه كلها تقريب!) |

**تشخيص البقية واضح من التوزيعات:** شبه كل الأخطاء عناقيد ±2..±5 حول الصفر —
عائلة تقريب (نقارن pred عشريًا ضد obs مكمَّم من XPS): التقريب المنضبط للتنبؤ
وقياس em الدقيق (319/320) هما بندا الجولة التالية، والقفزة متوقعة كبيرة.

## نمط التراكم (نفس الجولة): البقايا انجرافٌ دون-twip لا ضجيج

قياس الموضع المتراكم من مرساة الفقرة (بدل فروق الأزواج) — ‏`ACCUM=0` للقديم:
النتائج تحسنت حيث الخطوة دقيقة (jalsa ‏65→76.6، ‏masjid ‏78.3→81.7) وانكشف
انجراف منهجي صغير حيث لا (dawra ‏+2..+8، ‏ahadith ‏+3..+6، ‏tadris ‏−2) —
أي أن pitch المتنبأ صحيح إلى ±0.3–1 twip لكل سطر، والمتبقي **تكافؤ كمّ رأسي**:
تقريب Word الداخلي للخطوة وem الفعلي (319/320 من FontRenderingEmSize) —
بند الجولة التالية بفحص XPS الخام، لا معايرة عمياء.

## القاعدة الرأسية 4 (فحص XPS الخام): شبكة الطابعة 600dpi

قراءة `FontRenderingEmSize` و`OriginY` **العائمة** قبل أي تقريب حسمت الكمّ:

- **‏em يُكمَّم لعدد صحيح من نقاط 600dpi** ‏(نقطة = 2.4 twips): ‏16pt ترسم
  ‏15.96pt = ‏**133 نقطة** (‏133×72/600)، ‏20pt ← ‏**167** = ‏20.04pt، ‏15pt ← ‏125
  تام — ثلاث شهادات بلا استثناء. (يفسر «em=319» الملغز في truth.json.)
- **خطوات الأسطر المرصودة أعداد صحيحة من النقاط**: ‏477.6tw=‏199، ‏549.6=‏229،
  ‏597.6=‏249، ‏628.8=‏262، ‏652.8=‏272.

## القاعدة الرأسية 5 (قيد التحقيق): ارتفاع auto = ‏max(خطوة الخط، حبر السطر)

مزيج العناقيد يكذّب «خطوة ثابتة للفقرة»: ‏tadris ‏199×117 **و**200×62 بنفس
الإعدادات؛ ‏jalsa بذيل موجب طويل؛ ‏ahadith المشكول +1 غالبة. التفسير الموحد:
سطرٌ يعلو حبرُه (حركات متراكمة، مدود عالية) يرفع ارتفاعه فوق خطوة الخط —
دلالة «atLeast» الضمنية في auto. ‏**التنبؤ الدقيق يتطلب امتدادات الغليفات
الرأسية (HB glyph extents) لكل سطر** — جولة الترصيف الرأسي 2.
‏(A/B: نموذج الشبكة الصرف `GRID600=1` أدنى من العائم في التراكم — لأن الانجراف
الحقيقي ليس ثابتًا بل محتوى-حساس؛ يبقى علمًا حتى يكتمل نموذج الحبر.)

## نتيجة سلبية مؤسِّسة: الشبكة رأسية-فقط — الأفقي بالمقاييس المثالية

تطبيق تكميم em ‏(133 نقطة) على الأعراض الأفقية **نتيجة سلبية قاطعة**
‏(jalsa ‏82.8%←65.8%، ‏dawra ‏96.6%←81.7%، وكل الكتب سالبة أو محايدة) —
تتكامل مع تكذيب ADV_SCALE سابقًا: **‏Word يقيس النص أفقيًا بالمقاييس المثالية
الكسرية (كما HB تمامًا)، بينما شبكة 600dpi تحكم الترصيف الرأسي وحجم الرسم
فقط.** نظامان فرعيان مستقلان — الحدّيات الأفقية (±1%) ليست من الشبكة إذن،
وبابها الوحيد المتبقي فروق kerning/التقريب في مجاميع Uniscribe الداخلية.

## الجولة الرأسية 2: القاعدة 5 حُسمت — ‏max على runs السطر (والحبر بريء)

- **نتيجة سلبية:** فرضية «الحبر يرفع السطر» كُذبت مباشرة: امتدادات الغليفات
  (HB ‏y_offset + ‏glyf extents) في tadris لا تقترب من مقاييس trado الضخمة
  (أقصى صعود 95 نقطة مقابل 132 للخط) — العناقيد الأعلى ليست من الحركات.
- **القاعدة 5 النهائية:** خطوة السطر = ‏**max على runs السطر** من
  ‏(hheaPitch(خط الـrun) × em الـrun) — عنقودا tadris ‏203/204 = أسطر فيها
  ‏runs بولد (‏tradbdo ‏hhea ‏1.5327 × ‏133 = ‏203.85 ✓). مقاييس كل run تؤخذ
  من **subset المفكوك نفسه** (مطابقة «الأصل» بالعائلة تخلط العادي بالبولد).
- **التكميم:** الخطوة تُحسب عند **em المثالي** غير المكمم (تناوب tadris
  ‏199/200 بمتوسط 199.28 = ‏1.4946×133.33) — وem الحقيقة (319) يُعاد لأقرب
  نصف نقطة. التكميم النقطي للـbaseline المتراكم (v3) أضرّ أكثر مما نفع —
  الأفضل قياسًا (v4): خطوة عائمة تُجمع بلا تكميم.
- **خط الأساس الرأسي 2 (‏VMODE=4، ‏TOL=نقطة):** ‏muqtarah ‏97.9%، ‏jalsa
  ‏96.7%، ‏masjid ‏94.3%، ‏tadris ‏91.0%، ‏dawra ‏82.4%، ‏ahadith ‏56.6%
  (انحياز +1 نقطة منهجي خاص بخط Jazeera — بند مفتوح: مقياس رأسي بديل
  للخطوط بلا OS/2-typo متسق؟).

## بند مفتوح مضيَّق: شذوذ ahadith الرأسي (+1 نقطة)

فُحص وضُيِّق: مقاييس subsets المضمنة **مطابقة** لملفنا (‏hhea ‏1.6900/upem1000)،
والنموذج يعطي 261.0 نقطة (‏1.69×133.33×278/240) والحقيقة 262 — ‏+1 نقطة
منهجية على الخطوة المضروبة، بينما **كتاب Light بنفس العائلة سليم 97.9%**
‏(line=259). حُسم التشخيص تجريبيًا (‏VMODE=5): ‏**ceil على المفرد قبل المضاعف يصلح
ahadith ‏(56.6%←88.7%) ويدمر كل كتب upem=2048** ‏(jalsa ‏7.6%، ‏muqtarah
‏29.2% — وLight نفس العائلة!). الفارق البنيوي الوحيد: ‏**Regular ‏upem=1000**.
الفرضية المضيَّقة: مسار تكميم مقاييس Word يمر بوحدات الخط (تقريب صحيح في
فضاء units يذوب عند upem=2048 ويظهر عند 1000) — تُختبر بخط ثالث upem=1000
قبل أي شرطية في النموذج. الافتراضي يبقى v4.

## بنود مفتوحة

- تقريب pitch (أرضية/تقريب/تراكم baseline بدل فروق مستقلة؟ — يُقاس).
- حدود الفقرات (before/after + contextualSpacing) — v2.
- فواصل الصفحات والأعمدة، ‏keepNext/keepLines/widowControl — v3.
- الأسطر مختلطة الأحجام (max على runs السطر) — مستبعدة من v1 عمدًا.

## حسم فرضية upem=1000 بالتكذيب — ومولد المستندات المتحكم به وُلد

بُني `tools/word-oracle/gen_test_docx.mjs` (بذرة مولد الـcorpus): مستند مصفوفي
(Amiri ‏upem=1000/pitch=1.758 × ‏Traditional Arabic ضابطًا) × line ∈ {240،259،278}
بنصوص فريدة (فخ النص المكرر يفسد المحاذاة — دُوّن). النتيجة القاطعة:

- ‏**Amiri ‏v4: ‏95.74%** وceil (v5) يدمره إلى 44.7% — **فرضية «تكميم فضاء
  الوحدات عند upem=1000» مكذوبة**؛ الضابط trado ‏97.1% ✓.
- ⇒ شذوذ ahadith (+1 نقطة) **خاص بذلك الكتاب** لا بخط ولا upem — آخر
  الفرضيات: خاصية مستندية غير مقروءة فيه (يفحص XML كاملًا لاحقًا). أصغر
  جيب متبقٍ (53 زوجًا).
- **المكسب الاستراتيجي:** خط أنابيب المولد المتحكم به مثبت نهاية-لنهاية
  (توليد ← Word ← حقيقة ← قياس في دقيقة) — بوابة corpus الكشيدة وكل فرضية
  قادمة تُختبر بمستند مفصَّل على مقاسها.

**الحسم النهائي لشذوذ ahadith (مولد، تجربة 2):** ‏Jazeera-Regular في مستند
متحكم به بنفس الشروط (‏line=278، ‏16pt) = ‏**94.74% بالنموذج القياسي v4** —
لا +1 نقطة. ⇒ النموذج والخط بريئان معًا؛ العلة **خاصية داخل ملف ahadith**
(نمط/docDefaults غير مقروء) — بند صغير معزول (53 زوجًا) يُفحص بمقارنة XML
الكتاب ضد المستند المولد.

## القاعدة الرأسية 6: حدود الفقرات — انهيار الفواصل (max لا الجمع)

قياس أزواج الفقرات المتلاصقة (نفس الصفحة): ‏Δ = خطوة السطر الأول للاحقة +
**‏max(after السابقة، ‏before اللاحقة)** — الجمعُ الساذج خلّف عناقيد −120
(بمقدار before بالضبط) في masjid، وقاعدة الانهيار أزالتها كلها.
**خط أساس الحدود:** ‏muqtarah ‏**100%**، ‏jalsa ‏98.5%، ‏tadris ‏96.1%،
‏ahadith ‏86.1%، ‏masjid ‏79.8%، ‏dawra ‏79.0%. ‏(‏BGAP=sum للـA/B.)
البقايا: masjid ‏±3 و±20 (أنماط فرعية تُشرَّح)، وdawra ‏±5-8 (عائلة انجرافه).

**تشريح بقايا حدود masjid (‏BFOR=1):** عنقودا الفشل نمطان نظيفان:
‏(أ) ‏−3 (عائلة الانجراف المعروفة)؛ ‏(ب) ‏**+19..+21 حصريًا بين فقرات النمط
«a4» بلا فواصل** — والزيادة ≈ فرق خطوة نصف نقطة حجم (‏1.8916×10=18.9)،
بصمة **مشاركة تنسيق علامة الفقرة (rPr داخل pPr، ‏szCs أكبر) في ارتفاع
السطر** — قصة Word الكلاسيكية؛ الفحص: قراءة rPr علامة الفقرة في النموذج
وإدخالها في max القاعدة 5. بند الجولة القادمة الأول.

**تتبع عنقود a4 (+19/21):** ‏a4 = ‏List Paragraph بلا rPr حجم (فرضية علامة
الفقرة بُنيت — ‏markEmTwips في النموذج — ولم تصب هنا: ‏mark=240 أدنى).
الفجوة 586 = ‏**1.8916×310 بالضبط** ⇒ الفرضية التالية المضيَّقة: **حجم rPr
في مستوى الترقيم (‏lvl/rPr ‏sz=31 = ‏15.5pt) يشارك في ارتفاع سطر عنصر
القائمة** عبر علامة القائمة المرسومة — يُفحص بقراءة lvl/rPr في النموذج
والتحقق من em علامة الحقيقة (هل استُبعدت من rs؟). بند أول قادم.

**نتيجتان سلبيتان لعنقود a4 (+20):** ‏(أ) علامة الفقرة markEm — محايدة
(علامة a4 ‏240 أدنى)؛ ‏(ب) طيّ lvl/rPr.sz في ارتفاع السطر — أضرّ tadris
‏(96.1→93.7) فتُرك الحقل مكشوفًا بلا مشاركة. اللغز الباقي بتوقيعه الدقيق:
‏586 = ‏1.8916(adwa)×310 — حجم 15.5pt بخط المتن من مصدر غير معلوم بعد
(ليس نمطًا ولا علامة ولا lvl) — يُفحص بمستند مولّد بقائمة مطابقة لتنسيق a4.

**استنفاد تحليلي لعنقود a4 (ثلاث نتائج سلبية إضافية):** ‏sz=szCs=30 في
runs الفقرات (فرضية sz-اللاتيني ماتت)؛ سطر الحقيقة يحمل runs مسافة بخطوط
أخرى (Simplified Arabic/Arial) لكن خطواتها (حتى subsets وwin) ≤ ‏498 —
وmax(hhea, win) مرفوضة (متساويان في كل خطوط العينة). التوقيع 586 لا يفسره
أي مقياس لأي خط على السطر. ⇒ **الحسم الوحيد المتبقي: مستند مولَّد بقائمة
مطابقة لتنسيق a4 حرفيًا** (numbering + List Paragraph + adwa@15pt) —
أول بند في الجولة القادمة، والمولد جاهز (يلزم توسيعه بnumbering.xml).

## حصيلة بحث GDI (‏Clean-room ‏2026-07-13): تشريح المسارين — وVDMX محكوم عليه بالقياس

تقرير الوكيل (‏OpenType/recom، ‏Wine ‏freetype.c، ‏ReactOS، ‏MulDiv): ‏GDI يبني
‏tm من **‏OS/2-win** وexternalLeading = ‏max(0، ‏hhea−win) — مكافئ لقاعدتنا
‏max(win, hhea)؛ والتقريب round-half-up (‏MulDiv)؛ و**‏VDMX يتجاوز التحجيم**
عند ppem المسجل؛ وem الرسم = ‏MulDiv(pt،600،72) — يوثق رسميًا اكتشافنا 133.

**محاكمة VDMX على حقيقتنا** (‏trado@133=199، ‏167=250؛ ‏tradbdo@133=204؛
‏jazeera@133=222): يطابق 199 و**يؤكد عنقود البولد 204 حرفيًا**، لكنه يخالف
‏dawra (‏VDMX ‏250 ≠ الحقيقة 249) وjazeera ‏(222+45 ≠ ‏~226 المرصودة).
⇒ **‏Word ‏2013+/compat15 لا يمر بمسار GDI/VDMX في حساب الترصيف** —
النموذج الكسري المثالي (v4) يبقى الأصح؛ ‏VDMX/tm مسار الرسم القديم فقط.
(اكتساب جانبي موثق: تعريف ‏276=«1.15×المفرد» نص ECMA حرفيًا؛ وODF/LO
يقران بأن خوارزمية Word ‏2013+ «غير موثقة» — حقيقتنا XPS هي المرجع.)

## 🏆 لغز a4 (‏586) محلول — تقرير OnlyOffice/LO + الفحص المحلي

- المحركان يوثقان: تنسيق **علامة الترقيم يُدمج من rPr علامة الفقرة**
  (‏GetNumberingTextPr في sdkjs؛ ‏APPLY_PARAGRAPH_MARK_FORMAT_TO_NUMBERING
  في LO — يفعَّل لكل DOCX)، و**العلامة ترفع ascent السطر فقط لا descent**
  (‏sdkjs ‏Paragraph_Recalculate.js:3895 بتعليق يوثق أنه سلوك Word).
- الفحص المحلي: علامات فقرات a4 تحمل **‏sz=32 ×10 و34 ×12** (المتن 30).
- **التحقق الرقمي**: ‏Δ = ‏descent(em300) + ‏ascent(em320)
  = ‏1589/2048×300 + ‏2267/2048×320 = ‏232.8 + ‏354.2 = ‏**587.0**
  والمرصود 586–588 ✓ — القاعدة الرأسية 7.
- مكاسب موثقة إضافية من التقرير: صيغة المحركين = ‏max(hheaTotal، ‏winTotal)
  (تطابق نموذجنا)؛ المسافات لا ترفع السطر (‏IGNORE_TABS_AND_BLANKS — يوافق
  فلترنا)؛ شذوذ CJK ‏codepage ‏(×1.27/1.3)؛ وWord يقيس RTL بشريحة CS ‏(szCs).
- **التنفيذ (أول بند قادم)**: فصل ascent/descent في العدّة والنموذج
  (‏fontVerticalPitch ← ‏{asc، ‏desc، ‏gap})، والعلامة (بحجم rPr علامة
  الفقرة/lvl، شريحة CS) ترفع ascent سطرها فقط — يتوقع إغلاق +20 (10 أزواج)
  ورفع حدود masjid فوق 90%.

## القاعدة 7 منفذة (v7 افتراضي): الفصل asc/desc — وقفزة شاملة

‏Δ(a←b) = ‏desc(a) + gap(a) + (asc+desc+gap)(a)×(m−1) + ‏asc(b)؛ العلامة
ترفع asc **أول سطر** الفقرة المعدودة فقط (تصحيحان مقيسان: الرفع كان يسري
على كل الأسطر فأسقط muqtarah؛ وgap يدخل المضاعف — انجراف +0.8/سطر بدونه).

| | داخلي | حدود |
|---|---|---|
| tadris | **96.52** (كان 91.0) | **100%** |
| muqtarah | 97.92 | **100%** |
| jalsa | 96.20 | 98.51 |
| **dawra** | **95.95 (كان 82.4! — الفصل حل انجرافه)** | 92.98 |
| masjid | 92.68 (−1.6) | 77.30 (عناقيد ±21 متناظرة — إسناد رفع العلامة بين الزوج، خيط مفتوح) |
| ahadith | 56.60 (خاصيته الكامنة) | 86.11 |

**تضييق ±21 (‏masjid a4) بعد v7:** ليست علامةً (الأزواج الفاشلة علامتها
= متنها) وليست إضافة ثابتة بل **نسبة**: ‏587/567 و625/605 كلاهما ≈
‏(sz+1)/sz — كأن خطوة عناصر القوائم تُحسب عند **‏sz+نصف نقطة**. ‏a4 بلا
spacing مباشر (‏contextualSpacing فقط) وsectPr يحمل `docGrid linePitch=360`
(لا يفسرها حسابيًا بمفرده). فرضيتان للجولة القادمة: snapToGrid تفاعل جزئي،
أو قاعدة قياس علامة القائمة بشريحة CS مقابل ASCII (فرق تقرير OnlyOffice) —
تُفحص بمستند مولّد بقائمة نقطية مطابقة (بذرة المولد جاهزة، ينقصه numbering).

## 🏆 لغز 586 محلول نهائيًا (القاعدة 7-ب): خط العلامة بشريحة نصّها

المستند المولّد بقائمة مطابقة (bullet/Symbol) **لم يستنسخ** الظاهرة (فجوات
العناصر = الضوابط) — فانحصرت العلة في ترقيم masjid الفعلي: ‏decimal «‏%1.»
وأرقامه **لاتينية الشريحة** ⇒ تُرسم بخط **‏rFonts ascii من rPr علامة
الفقرة = ‏Simplified Arabic** — و‏asc(simpo) = ‏1.1797em العملاق:
‏desc(adwa)×300 + ‏asc(simpo)×300 = ‏**586.7** والمرصود 586–588 ✓ تام.
(التوقيع «adwa×310» كان صدفة عددية — والحل الحقيقي شريحة الخط.)
التنفيذ: ‏markAsciiFamily في النموذج + ‏famMet في العدّة — ‏masjid حدود
‏77.3←**‏81.6%** وصفر حراك في بقية الكتب (سببية نظيفة). المتبقي هناك:
عائلة −3 الانجرافية و−21 معاكسة قليلة (رفعٌ زائد حيث علامة أصغر — تدقيق
شرط الرفع لاحقًا).

## القاعدة الرأسية 8: بداية الصفحة — baseline الأول = الهامش العلوي + ascent

مقياس «بداية الصفحة» أضيف للعدّة (‏marTop/marBottom في SectionGeometry):
‏**jalsa ‏100% ‏(1440+320=1760 تام) وtadris ‏100% ‏(1134+318≈1450)** ✓،
‏dawra ‏5/6، ‏masjid ‏10/15 (‏+5 عائلة أنصاف النقاط). المتبقي نمط واحد:
إزاحة موجبة ثابتة (‏ahadith ‏+112، ‏muqtarah ‏+104) = **دفع الهيدر العميق**
(محتوى الهيدر أسفل من الهامش العلوي يدفع بداية المتن — قاعدة 8-ب تتطلب
ارتفاع محتوى الهيدر: بند لاحق محدد).

## ★ مُحكِّم الصفحة الكاملة (أول التحام): تنبؤ متسلسل 8→7→6 بلا مراسٍ

تركيب القواعد تنبؤًا واحدًا من الهامش العلوي حتى آخر baseline (المرساة
الوحيدة: لا شيء بعد البداية):
‏**tadris ‏100%** على سلاسله، ‏dawra ‏85.2%، ‏jalsa ‏64.9% (‏+5 موحد =
تراكم دون-النقطة عبر السلسلة)، ‏masjid ‏41% (خليط عائلاته)، ‏vtest3 ‏58%.
**الدرس البنيوي**: التركيب يضخّم الانحيازات الدقيقة (±1–2tw/خطوة تصبح +5
بعد سطور) — فجبهة «تكافؤ الكم الرأسي الدقيق» (أنصاف النقاط/دفع الهيدر)
هي الفاصل بين هذه الأرقام و100% صفحةً كاملة. المقياس الرابع الآن قائم
في العدّة ويقيس كل جولة قادمة تلقائيًا.

## 🏆 القاعدة 8-ج: externalLeading فوق أول سطر الصفحة — الإزاحتان الكامنتان انحلتا

المطاردة: ‏muqtarah/ahadith بدايتا صفحاتهما +104/+112 فوق marTop+asc —
والنسبة ثابتة **‏0.35em**. الحل: ‏extLeading = ‏hheaTotal − winTotal
(‏jazeera **الوحيد** الذي يفترقان فيه: 0.342em — بقية خطوطنا ext≈0) —
وقاعدة LO الموثقة: «‏leading يُضاف فوق السطر». الصيغة:
**بداية الصفحة = ‏marTop + ‏(hheaTotal − winDescent)×em** ‏(= ext + winAsc).
النتائج: بدايات muqtarah وahadith ‏**100%**، الإجمالي 47/49 عبر الستة؛
الصفحة الكاملة: ‏ahadith ‏0←**87.5%**، ‏muqtarah ‏0←76%، ‏masjid ‏41→57.5%
(‏adwa ‏ext=0.009em حسّن بداياته أيضًا)، ‏tadris ‏100% ثابت.
‏(fontVertMetrics تقرأ الآن OS/2 winDescent؛ ‏lineMet يصدّر ascStart.)

**حد ahadith الداخلي (الجيب المجهري الأخير):** بعد 8-ج بقي +1 نقطة/زوج
(‏obs = ‏262 نقطة صحيحة، النموذج 261.07). مسحت البدائل كلها بالقياس:
‏line=279؟ (‏278 محسومة وثائقيًا)؛ ‏gap الـsubset؟ (‏34 مؤكد خامًا)؛
‏VDMX ‏(222+ext=267 ✗)؛ تكميم المفرد قبل المضاعف بكل اتجاهات التقريب ✗.
‏53 زوجًا — أصغر مجهول رأسي، مرشحه الأخير نمذجة «‏المفرد» الحرفية لمحرك
Word ‏2013+ لهذا القالب (‏line=278) بمستند مولّد بمصفوفة line دقيقة
‏(277/278/279/280) — تجربة جاهزة للجولة القادمة بالمولد القائم.

**تجربة مصفوفة line ‏276–280 (المولد، ‏Jazeera-Regular):** المرصود يطابق
النموذج عبر القيم الخمس (‏277: ‏260.00 مقابل 260.07 تام؛ والبقية تناوب
±0.3 نقطة حول النموذج) — **ولا يستنسخ 262 الكتاب حتى عند 278 نفسها**.
إثبات نفي ثانٍ مستقل (بعد vtest2): نموذجنا صحيح لهذا الخط وهذا القالب،
وعلة ahadith كامنة في ملفه (خاصية لم تُقرأ بعد — مقارنة XML شاملة بين
الكتاب والمستند المولد هي أداة الحسم الأخيرة المتبقية).

**النفي الثالث (‏vtest5، ‏docGrid=360):** الشبكة محايدة (المرصود 626.6 ≈
النموذج 626.4) — لا تستنسخ 262. الحصر الآن ثلاثي التحكم: خط ✓ حجم ✓
‏line ✓ ‏compat ✓ ‏docGrid ✓ كلها مطابقة للنموذج في المولد — فالخاصية
الكامنة في طبقة أعمق (docDefaults rPr الكامل؟ نمط Normal الكتاب؟
‏themeFontLang؟) — **‏diff منهجي بين XML الكتاب وvtest5 هو الحسم**، ومساحة
البحث الآن ضيقة جدًا بفضل النفيات الثلاثة.

**حصار خاصية ahadith — الجولة الليلية الكاملة (7 نفيات متحكم بها):**
‏kern/ligatures ✗ (‏vtest6: التوزيع مطابق)؛ ‏rPrDefault كاملًا ✗ (‏vtest7:
مطابق حرفيًا)؛ ‏sz اللاتيني + ‏ascii/hAnsi ✗ (‏vtest9 ≡ ‏vtest5)؛ ‏docGrid ✗؛
‏line ✗؛ الخط/الحجم ✗؛ وفرضية الحبر منفية هندسيًا لجزيرة أيضًا (ذروة
الحركات yMax=904 < ‏asc=1054). **المؤشر الوحيد الموجب**: ‏vtest8 (فقرات
الكتاب حرفيًا في هيكلنا الأدنى) أعطى 628–630 (كالكتاب) — لكن بعينة 5 أزواج
فقط. الخطوة المسماة التالية: ‏vtest8-موسع (‏15+ فقرة منقولة) للفصل الصارم
بين «‏w:p تحمل العلة» وضجيج العينة — وإن ثبت السفر فالمشتبه الأخير محتوى
الـruns الحرفي (مسافات بلا rPr؟ أحرف خاصة؟) يُنصَّف داخل الفقرة الواحدة.

## القاعدة 9: سقف نقطة الخطوة الأولى لفقرات «التباعد كالمدمج» — انحلال لغز 262 كاملًا

**الليلة الحاسمة (‏vtest10–16، ست طباعات متحكم بها):**

1. **‏vtest10 (‏vtest8-موسع، 30 فقرة منقولة حرفيًا):** السفر ثبت إحصائيًا —
   العنقود المهيمن 628×22 + 630×8 (توقيع الكتاب) في هيكلنا الأدنى.
2. **‏vtest11 (مصفوفة pPr رباعية بفقرة p25):** ‏bidi ✗ وjc ✗؛ والمفاجأة:
   ‏spacing صريح line=240 يعطي **540** (المفرد الخام ✓ النموذج) بينما الغياب
   التام (بلا styles.xml) يعطي عنقود الكتاب — **افتراضي Word المدمج في REF-1
   = ‏after=160 line=278** (أُثبت لاحقًا بانطباق الميل على 278).
3. **‏vtest13/14 (عزل المحتوى، 7 أذرع):** التشكيل الكامل ✗، أقواس ﴿﴾ TNR ✗،
   تقطيع runs ✗، ‏enableOpenTypeFeatures ✗ (تطابق حرفي حتى آخر tw).
   والاكتشاف الموضعي: **أول/أوسط خطوات الفقرة = النموذج؛ الأخيرة مقروءة
   أعلى** — أول خيط نحو «ثابتٍ لكل فقرة».
4. **‏vtest15 (مصفوفة العلامة ¶):** علامة sz=72 العملاقة **لم تحرك السطر
   الأخير إطلاقًا** (M0=M1=M2 حرفيًا) — مشاركة العلامة ميتة خارج سياق
   الترقيم. وأطوال 2–13 سطرًا أعطت الانحدار الفاصل:
   **‏T(n) = ‏n×خطوة + ‏2.4tw** للتباعد الموروث، و**‏c=0** للمباشر (M3).
5. **‏vtest16 (تنصيف هيكل×نص):** الأذرع الأربعة متطابقة، والإجماليات تدور
   بدورة طور خماسية [1880،1884،1882،1882،1880] متوسطها **‏784.00 نقطة
   بالضبط** لثلاث خطوات — الثابت حقيقي والطور يوزعه على المواضع.

**الصيغة (بالنقاط 600dpi):** فقرة تباعدها موروث من docDefaults بقيمة
الافتراضي المدمج (‏line=278):
**‏T(n) = ‏ceil(خطوة) + ‏(n−1)×خطوة** — أي سقفٌ لخطوة السطر الأول فقط.
‏ahadith: ‏ceil(261.011)−261.011 = ‏+0.989 نقطة = ‏2.37tw (المقيس 2.4 ✓)،
وتفسر إجماليات vtest15 كلها حتى n=12 (‏3133.1 متنبأة مقابل 7520tw مرصودة).

**حدود القاعدة (محاكمات نفي مثبتة):**
- المباشر في pPr لا يُسقَّف (‏vtest15-M3، ‏vtest5، ‏tadris/jalsa ‏100%).
- وراثة نمط مسمى لا تُسقَّف (تعميمها كسر masjid ‏92.7→75.6).
- ‏docDefaults بقيمة أخرى لا يُسقَّف: ‏muqtarah ‏(259، خطوة 228.033 نقطة
  بكسر شبه مطابق لكسر ahadith ‏261.011) **بلا رفع** — فالكسر ليس المميز؛
  القيمة 278≡المدمج هي. الآلية الدقيقة سؤال مفتوح (مسار «كالمدمج»؟)
  وتجربته القادمة: docDefaults=277/279/280 في هيكلنا.
- الحدود وبداية الصفحة لا تتأثران (تجربة الحدود: ‏86→31% بإقحامها).
- **درسٌ منهجي:** «‏+1 نقطة/زوج» و«الخطوة الأولى +4» و«الأخيرة +1» كلها
  كانت أقنعة لثابتٍ واحد لكل فقرة يطفو موضعيًا مع طور التكميم — فك
  الخليط {626/628} مقابل {628/630} هو ما كشف القناع.

**النتائج (‏lineSource في النموذج + ‏inhPlus في المحكّم):**
‏ahadith الداخلي **‏56.6→88.7%** (المتبقي +3×4 حدّي وشاذّا محاذاة)،
والخمسة الأخرى على ذراها بلا انتكاس (‏masjid ‏92.7، ‏muqtarah ‏97.9،
‏tadris ‏96.5 و100% صفحة كاملة، ‏jalsa ‏96.2، ‏dawra ‏96.0).

### تصويب القاعدة 9: قانون الكسر (مسبار docDefaults عشري القيم — vtest17)

بوابة «القيمة 278 حصرًا» كانت اجتهادًا مرحليًا؛ عشر طباعات مسبار
‏(docDefaults ‏line ∈ {240، 242، 243، 259، 260، 272، 274، 276، 277، 278}
في هيكلنا بstyles.xml أدنى) حسمت القانون الحقيقي:

**السقف ⇔ كسر الخطوة (بالنقاط) < ‏1/6** — بلا أي اعتبار للقيمة:
- مسقوفة: كسور ‏.011 ‏(278)، ‏.072 ‏(277)، ‏.111 ‏(260)، ‏.133 ‏(276)، ‏**.150 ‏(243)**
- حرة: ‏**.172 ‏(259)**، ‏.211 ‏(242)، ‏.256 ‏(274)، ‏.333 ‏(240)، ‏.378 ‏(272)

العتبة محسورة قياسًا في **‏(0.150، 0.172]** و‏1/6 = ‏0.1667 في وسطها
(آلية التكميم الداخلية — ‏LineServices؟ — سؤال مفتوح؛ فرضيات ceil/round
البسيطة نُفيت واحدة واحدة أعلاه). وتصويبان تابعان:

1. **‏muqtarah لم يكن استثناء قط**: كسره بمقاييس Light النقية = ‏0.974
   (حر ✓)؛ قيمة stepV7 «‏228.033» كانت ملوثة بmax-عبر-runs — لذا حساب
   القاعدة 9 في المحكّم يستعمل الخطوة النقية بخط المتن حصرًا.
2. **النطاق**: كل line غير مباشر (docDefaults أو نمط مسمى) — ‏masjid
   (نمط 276 بكسر .92) لا يميز بين النطاقين فبقي التعميم الأنظف، وقابل
   للتكذيب متى ظهر كتاب نمطُه بكسر صغير.

المصفوفة الكاملة بعد قانون الكسر: بلا أي تغيير عن أرقام القاعدة 9
(‏ahadith ‏88.7% وبقية الكتب على ذراها) — القانون أعم والنتائج مصانة.

### شد العتبة وتعميم القانون عبر الخطوط (vtest18/19)

- **مسبارا ترادو (‏docDefaults ‏281/282):** الميل بترادو المثبَّت (‏1.4946)
  صفري الخطأ (‏±0.00tw) — **المدمج 278 عالمي عبر الخطوط**. وكسر ‏0.154
  حر بينما كسر جزيرة ‏0.150 مسقوف ⇒ **العتبة = 0.15 شاملةً**، محسورة
  قياسًا في ‏[0.150، 0.154).
- **تجربة قوائم dawra المولّدة (‏vtest18، ترادو 40/32):** حدود عناصر
  contextualSpacing = خطوة السطر النظيفة تمامًا (لا انكماش) — قاعدة
  الحدود سليمة. **وتشخيص علة dawra الحقيقية**: حدودها المرصودة 598–600
  = ‏1.4946×em (ترادو **العادي**) بينما تنبؤنا 613 = ‏1.5327 (‏subset
  **البولد** tradbdo) — علة **خلط مقاييس بولد/عادي** حول سطر العلامة،
  لا قاعدة ترصيف جديدة؛ حسمها بفحص runFonts الحقيقية لأسطر تلك الحدود
  (الجولة القادمة).
- عائلة masjid ‏±3..5 (المتبقية): عائلتان — ‏line=276 مباشر ‏before/after
  ‏120 (‏+3/+4 حدّية) وline=240 وراثة نمط (‏±3..12) — مؤجلة بترتيبها.
