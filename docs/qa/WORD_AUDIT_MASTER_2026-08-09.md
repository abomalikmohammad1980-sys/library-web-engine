# WORD AUDIT MASTER 2026-08-09

> سجل موحد يحفظ التقارير الأصلية كاملة، مرتبة باسم المصدر؛ أُنشئ أثناء تنظيف 2026-08-09.

---

## المصدر: word-final-audit-2026-08-08.md

# تدقيق التسليم النهائي — مسار Word

## المطلب ← الدليل

| المطلب | الدليل المثبت | الحالة |
|---|---|---|
| مطابقة الفهرس والرقم المطبوع (QA-004) | corpus «الطريق إلى القرآن»: 72 ورقة، adjusted حتى 73، أهداف 8/19/48؛ QA حي side/internal وصل Word 8/19/48 والورقة 7/18/47 | PASS |
| عدم إعلان 1/1 و100% أثناء المعاينة (QA-011) | المعاينة تستعمل الإجمالي الفيزيائي المحفوظ؛ QA حي أعاد الورقة 1 من 72 و1% قبل اكتمال التحويل | PASS |
| «منهاج مخيم» لا يفشل ولا يعتمد 59 كحقيقة (QA-012) | Word COM/WordPageMap = 41؛ browser estimate = 59 موسوم approximate ولا يحفظ؛ corpus DOM/scene/map واختبار القارئ | PASS |
| عدم دمج ص3 وص4 في «توحيد الحاكمية» | خريطة 32 ورقة: ص3 فقرات 2..7 ولا تحتوي «مقدمة الناشر»، ص4 8..17 وتبدأ بها؛ ص9 عنوان الكتاب | PASS |
| صورة ص3 السطرية | ملكية الفقرتين وأبعاد Word ‏503.4×513.133 و188×53.2، بلا ضغط responsive | PASS |
| PAGE داخل Content Control | footer corpus فقرتان وحقل PAGE في SDT؛ DOM يعرض 3 | PASS |
| الحواشي في «توحيد الحاكمية» | الحواشي 1..7 مملوكة للأوراق 8،9،10،13،23،24 | PASS |
| repeat table header في «منهاج» | Word يكرر رأس جدول الإيقاع على أوراق 11–14؛ scene يثبت 17 رأسًا فريدًا ويخرج نسخها في الصفحات التالية مع خصم ارتفاعها | PASS |
| الأوراق الفارغة في WordPageMap | `starts` للصفحات ذات الفقرات فقط؛ `pages` كامل 1..N و`-1/-1` للورقة الفارغة؛ service/client validator واحد | PASS |
| انعكاس عنوان «زهر الخمائل» | 7 رنات؛ قلب/blur/offset/alpha mask محفوظة في طبقة مطلقة لا تدخل النص أو القياس | PASS |
| contextualSpacing في «توحيد الحاكمية» | 165 وسمًا؛ أكثر من 100 فقرة فعالة، وإلغاء مستقل لـbefore/after عند تطابق النمط | PASS |
| before/afterAutospacing | Word OM: ‏5pt ثابتة في توحيد/إبهاج وعبر 16/25/40pt؛ 181 فقرة توحيد محسومة إلى 100twips | PASS |
| جدول داخل حاشية + cantSplit | الحاشية 219 في «الشامل في المسائل»: جدول 4 صفوف وكلها break-inside:avoid؛ synthetic مستقل | PASS |
| فواصل الأعمدة | corpus: فاصلان في «هموم وآلام» وستة في «زاد المجاهد2»؛ موضع محرفي + break-before:column بلا تغيير فقرات الخريطة | PASS |
| شبكة أسطر docGrid | Word COM/PDF: auto10=18pt، atLeast15=18pt، exact15=15pt، nosnap=11.25–12pt، auto30=36pt؛ وراثة snap وتطبيق المضاعفات مثبتان | PASS |

## العقود المرجعية

- `docs/word-behavior-spec/authoritative-word-page-map.md`
- `docs/word-behavior-spec/authoritative-vs-estimated-pagination.md`
- `docs/word-behavior-spec/inline-picture-page-map-alignment.md`
- `docs/word-behavior-spec/inline-picture-authoritative-extents.md`
- `docs/word-behavior-spec/content-control-page-fields.md`
- `docs/word-behavior-spec/footnote-block-tables.md`
- `docs/word-behavior-spec/column-breaks.md`
- `docs/word-behavior-spec/windows-word-page-map-service.md`

## نظافة التسليم

- لا علامات تعارض في `packages/` أو `app/src/` أو `docs/` أو `PROGRESS.md`.
- حُذفت مخرجات run9 المؤقتة (DOCX/PDF/page-map وسجلات Vite) من `tmp/`.
- لا توجد معالجة خاصة باسم كتاب؛ ملفات corpus بوابات انحدار فقط.
- بوابة Word الختامية: model/dom/scene/paint ‏225 اختبارًا PASS، مع 7/7
  service/pipeline وWord COM corpus ‏41/32 PASS،
  والبناء الكامل PASS بـ174 وحدة؛ QA-011 مؤيد كذلك
  بإعادة الاختبار الحية المسجلة في تقرير QA.

## نقطة الاستئناف الدقيقة

1. **محول التخزين الإنتاجي:** الخدمة ومنسق الرفع→الحفظ أصبحا جاهزين
   (Bearer/limits/timeout/retry/cleanup/fingerprint، وحفظ ذري يرفض stale،
   وcorpus 41/32). المتبقي على محول التخزين الفعلي تنفيذ `commitIfCurrent`
   بمعاملة قاعدة البيانات نفسها؛ العقد واختبار المخزن الذري مثبتان. عند عدم
   نجاح المقارنة يبقى القارئ `approximate` مع تنزيل الأصل ولا يدعي المطابقة.
2. **docGrid أُغلق بقياس لا بتخمين:** قياس Word COM/PDF والخوارزم والاختبارات
   موثقة في `docs/word-behavior-spec/document-grid-line-pitch.md`. لا يبقى فيه
   عمل مفتوح إلا corpus جديد يثبت حالة مختلفة.
# تحديث corpus عاجل — 2026-08-09

## سيرة الشيخ أبي أنس الشامي

- Word COM (قراءة فقط): 7 أوراق، 47 فقرة، مقطع واحد؛ أول فقرة متن مرئية في
  الورقة الثانية.
- OOXML: صورة VML كاملة الصفحة قبل `lastRenderedPageBreak` في الفقرة الأولى،
  `titlePg=true` بلا first header/footer، و`evenAndOddHeaders@val=0`.
- أُغلق تراكب المتن على الغلاف بملكية anchor السابقة لحد الصفحة وتطبيق الحد على
  أول فقرة. حُلّت علاقات الرأس والتذييل إلى الأجزاء الفعلية بدل ترك rId غير
  قابل للعرض.
- اختبار corpus scene يثبت ص1 بلا فقرات وبغلاف واحد، وبداية المتن وقصص الهامش
  من ص2. العدد المنشور يبقى من WordPageMap الصارمة، لا من تقدير scene.

## سرور.. بل أحزان

- OOXML: 42 فقرة `lineRule=auto,line=192`؛ لا exact/atLeast في متن المستند.
- أُلغي إصدار `line-height:0.8` الذي يصغر عن امتداد الغليفات العربية، وحُفظت
  `left/right` والبادئتان كقيم مادية مستقلة عن اتجاه RTL.
- corpus regression يثبت أكثر من 30 فقرة متأثرة، واختبار paint المباشر يثبت
  `normal` للقيمة 192 و`left` الفيزيائية.

## البوابة

- Word model/DOM/scene/paint/cover: 235/235، ثم اختبارا paint الجديدان 2/2.
- بناء TypeScript للحزم الخمس ناجح.
- لم يُعدّل أي ملف Word أصلي، ولم يُغلق Word المرئي للمستخدم.

## إبهاج أهل الصناعة — مرساة التذييل النسبية للفقرة (2026-08-09)

- WordPageMap الصارمة: 22 ورقة. قبل الإصلاح كان scene ينتج 268 ورقة لأن خط
  التذييل الأفقي (`extentH=1`, `relativeFrom=paragraph`, `offset=2`) عومل كأنه
  عند أعلى الصفحة، فصار حد المتن مساويًا للهامش العلوي.
- بعد الإصلاح: تقدير fallback = 33 ورقة، والخط في موضع التذييل الفعلي، والصفحة
  العادية تحمل أكثر من عشرة أسطر. الرقم الرسمي لا يزال 22 من WordPageMap.
- scene: 37/37 وبناء TypeScript للحزمة ناجح في البوابة المركزة.

## كشف استبدال الخط — 2026-08-09

- صار ناتج scene يفرق صراحة بين عائلة Word المطلوبة والعائلة التي قاس بها
  المورّد، ويصنف الحل exact/fallback/default. لا تغيير حسابيًا ولا خط مملوك.
- اختبار corpus «إبهاج» يثبت أن `Traditional Arabic` معلن مستبدلًا بخط الجزيرة،
  بدل أن يظهر التقدير المختلف كأنه fidelity كاملة.

## robustness — حد صفحات scene الصارم (2026-08-09)

- أُغلق تجاوز `maxPages` داخل الفقرة الطويلة وصف الجدول المجزأ وحدود الصفحات؛
  الفحص لم يعد مؤجلًا إلى نهاية الفقرة.
- اختبار seeded بآلاف الكلمات وأربع حدود صريحة يثبت حد 2 وحد 1 حرفيًا.

## performance — keepNext الخطي (2026-08-09)

- أثبت مسح corpus 272 حالة في زاد المجاهد2/2701 فقرة و8 في زهر الخمائل/2526.
- استُبدل slice/find المتكرر بفهرس Int32 خطي سابق للتصفيح، بلا تغيير دلالي.
- اختبار مباشر يثبت تجاوز الفراغ وخلايا الجدول والوصول للفقرة المتنية التالية.

## fidelity — حد w:kern في scene (2026-08-09)

- corpus يثبت قيم 2 و32 في كتب كبيرة؛ كانت طبقة DOM تحترمها وscene لا يفعل.
- صار HarfBuzz يتلقى `-kern` تحت الحد فقط، ويبقى طبيعيًا عند الحد وفوقه.
- اختبار الحدود 31/32/40 نصف نقطة وحالتي 0/غياب أخضر.

## fidelity — مقياس عرض المحارف w:w (2026-08-09)

- 92 رنًا في corpus تستعمل w:w؛ كان محفوظًا في النموذج وDOM فقط.
- صار scene يضغط تقدم الغليف والمسافة، وCanvas يفصل sx الأفقي عن sy الرأسي،
  وHTML يستخدم font-stretch. اختبار 50% يثبت العرض وثبات ارتفاع السطر.
# تدقيق gaps تابع — 2026-08-09

- بعد إغلاق محاذاة الجداول، فُحصت فجوات كبيرة محتملة فقط: لم يظهر `tblpPr`
  أو `tblCellSpacing` أو `tblOverlap` أو `gridBefore/gridAfter` في corpus الحالي.
- `vMerge` ظهر في fixtures المخصصة له فقط (أربع ملفات، ثلاث خلايا لكل ملف)،
  ولا يثبت regression في كتاب مستخدم جديد؛ بقي عقد DOM القائم ولم نضف تقريب scene.
- لم يوجد مقطع corpus فعال بـ`docGrid type=lines|linesAndChars`، لذلك لم نغيّر
  scene اعتمادًا على pitch غير فعّال.
- تصريحات `w:rPr/w:spacing` الخمس غير الصفرية في `sample-ahadith` موجودة في
  `styles.xml` فقط، ولم تصل إلى أي `EffectiveRun` مستخدم. نُفذت تجربة أولية ثم
  أزيلت كاملة حين فشل شرط الدليل؛ لا تُحسب خاصية غير مستخدمة إنجازًا.
- لم تظهر حالات `wrapTight/wrapThrough/wrapTopAndBottom`. النتيجة: لا gap تالٍ
  آمن واسع الأثر ثبت من هذا المسح، ولا تعديل تخميني.

## تدقيق robustness ختامي — 2026-08-09

- أعيد فحص الفجوات المعلنة مقابل التنفيذ الحالي. اتضح أن سجل `cantSplit` كان
  تاريخيًا: scene يملك الآن `SceneTable/SceneRow` ويقيس أقصى ارتفاع خلايا الصف
  وينقل الصف كاملًا أو يشطر الصف الأطول من الصفحة دون فقد. صُحح السجل، ولم يتغير
  سلوك المنتج.
- لم يثبت corpus حالة جديدة لـwrap Tight/Through/TopAndBottom أو page border
  فني يمكن رسمه من OOXML بلا أصل زخرفي؛ لم نضف أي تقريب.
- بوابة Word الكاملة المركزة: **11 ملفًا / 185 اختبارًا**؛ بناء الحزم الست
  `ooxml-model/layout/scene/paint/ooxml-dom/word-cover` ناجح.
- بوابة أصول الإصدار (210,679,842 بايتًا عبر 3 جذور) نجحت ثلاث مرات: تشغيل
  بارد 13,920ms ثم تشغيلان دافئان 278/279ms. لا تسرب فشل أو إعادة بناء أو تغير
  بصمة بين التشغيلات؛ فرق التشغيل البارد هو قراءة نظام الملفات لأول مرة.

---

## المصدر: word-final-release-audit-2026-08-09.md

# تدقيق إصدار Word النهائي — 2026-08-09

## النطاق

دُققت أعمال Word الستة المنشورة في ثلاث نسخ مستقلة من الحزمة الحالية:

- `app/public`
- `app/dist`
- `alpha-publish/pages-dist`

المصدر المرجعي هو `tmp/publish-staging-2026-08-09/word-artifacts-manifest.json`.
لم يُعدّل أي DOCX أو PDF أو WordPageMap في هذا التدقيق.

## خرائط Word والبصمات

- 6/6 أعمال Word موجودة في كل جذر، أي 18 ربطًا مفحوصًا.
- بصمة SHA-256 لكل DOCX تطابق بصمتها في manifest المنشور في الجذور الثلاثة.
- خرائط Word الست تطابق خريطة staging دلاليًا؛ اختلاف whitespace في خريطة
  «الحاسم» لا يغير JSON أو صفًا واحدًا.
- لكل جذر **663** صف صفحة: 22 + 357 + 162 + 48 + 40 + 34.
- `physicalPage` متصل 1..totalPages، وكل صف يملك `adjustedPage` صحيحًا؛ مجموع
  الصفوف المتحقق منها عبر الجذور الثلاثة **1989**.
- الرابط السادس ليس مفقودًا: خريطة «الحاسم» محفوظة في
  `library/published/word-page-maps/b7fe0ed20f11.json`، بينما الخرائط الخمس
  الأخرى تحت `assets`. بوابة الإصدار تحل المسار من manifest ولا تعتمد نمط الاسم.

بوابة قابلة للإعادة: `tools/verify-published-word-release.mjs`؛ نتيجتها `pass`.

## الفحص البصري

رُندرت أول/وسط/آخر صفحة فعلية من PDF المرتبط بكل عمل، بإجمالي **18 صفحة**،
وفُحصت الصور الأصلية لا استخراج النص وحده. العينات محفوظة مؤقتًا تحت
`tmp/pdfs/word-release-final-2026-08-09/`.

- إبهاج: 1/11/22 — الغلاف والصورة والرأس والتذييل والنص سليمة.
- خالعة القلوب: 1/17/34 — الآيات والشعر والحواشي والترقيم بلا تداخل.
- الحاسم: 1/179/357 — الغلاف والمتن والصفحة الختامية سليمة.
- حم لا ينصرون: 1/32/64 — الغلاف والمتن سليم؛ الصفحة 64 بيضاء في PDF الأصلي.
- تمجيس الجذور: 1/32/64 — الغلاف والمتن سليم؛ الصفحة 64 بيضاء في PDF الأصلي.
- الدولة الإسلامية: 1/120/240 — الغلاف والمتن سليم؛ الصفحة 240 بيضاء في PDF الأصلي.

لا قص أو تداخل أو أحرف عربية مكسورة في العينات النصية. أصدر Poppler تحذير
fallback لـSymbol/ArialUnicode في بعض الملفات، لكن الفحص المرئي لم يظهر غليفًا
مفقودًا في الصفحات المختارة.

## اختلاف عدد صفحات PDF

عدد PDF يساوي WordPageMap في إبهاج (22)، الحاسم (357)، وخالعة القلوب (34).
أما PDFs الأصلية للأعمال الثلاثة الأخرى فأعدادها 240/64/64 مقابل خرائط Word
162/48/40. هذه ليست صفحات أنشأها التطبيق: بصمات PDF المنشورة ثابتة، والفحص
وجد صفحات فارغة ضمن/نهاية الأصول (آخر صفحة نصية 238/63/62). تبقى WordPageMap
سلطة ترقيم نسخة Word، ولا يجوز استبدال عددها بعدد PDF المختلف.

## البوابات

- `verify-publish-word-artifacts.ps1`: 6 مستندات / 663 صفحة — pass.
- `verify-published-word-release.mjs`: 3 جذور / 18 ربطًا / 1989 صفًا — pass.
- اختبارات seed/property/namespace المنشورة: 3 ملفات / 14 اختبارًا — pass.
- بوابة محرك Word السابقة: 16 ملفًا / 340 اختبارًا وبناء 6 حزم — pass.

النتيجة: إصدار Word الحالي متسق بين المصدر وapp/dist وPages، ولا عيب دقيق
يستوجب تعديل artifact في هذه الجولة.

---

## المصدر: word-final-requirement-gate-2026-08-09.md

# بوابة متطلبات Word النهائية — 2026-08-09

## القرار

- **GO لإصدار Alpha الحالي والأعمال المنشورة الـ23.** خرائط Word الست الصارمة،
  أصول التنزيل، والصيغ المادية متطابقة بين public/dist/pages-dist، ولا يوجد عيب
  مفتوح مثبت في عينات الإصدار يمنع استعماله.
- **NO-GO لإعلان المرحلة 2 «مطابقة Word الكاملة» منتهية.** هذا ليس فشلًا في
  الإصدار الحالي؛ بل التزام صادق بمعيار الخطة الأعلى: ما زالت المقارنة البصرية
  لكل صفحة من corpus، DOCX حقيقي يحوي OMML، والحالات الفنية النادرة ذات الأصول
  غير المتوافرة بوابات قبول لم تُستوفَ بعد.

## مطابقة المتطلبات بالدليل الحالي

| المحور | الدليل الحالي | الحكم |
|---|---|---|
| الصفحة والترقيم | WordPageMap: 6 أعمال، 663 صفحة فريدة، 1989 صفًا عبر 3 جذور؛ physical متصل وadjusted صحيح | GO للإصدار |
| أول/وسط/آخر صفحة | 18/18 صورة رندر أعيدت ببصمة SHA-256 مطابقة؛ فحص بصري بلا قص/تداخل | GO للعينة، لا يغني عن كل الصفحات |
| الرأس/التذييل/الغلاف | corpus سيرة أبي أنس وإبهاج وتوحيد الحاكمية + اختبارات scene/DOM | GO للعينات |
| الجداول والحواشي | repeat header وcantSplit وصفوف متعددة الخلايا وجداول الحواشي مغطاة | GO للعينات |
| المعادلات OMML | model وMathML الدلالي: 4 اختبارات مباشرة ضمن 75/75 مركزة | GO للعقد التركيبي؛ corpus DOCX حقيقي باقٍ |
| الإطارات والالتفاف | الأنواع الممثلة مغطاة؛ لا corpus لـTight/Through/TopAndBottom في المسح الأخير، والإطار الفني يحتاج أصل زخرفته | NO-GO للإغلاق العام |
| الصيغ والتنزيل | 23 عملًا، 29 أصلًا، 87 إحالة عبر 3 جذور؛ الحجم/SHA/signature/path كلها صحيحة | GO للإصدار |
| الاختبارات | 253 ملفًا و1101 اختبارًا ناجحًا؛ اختبار خدمة Word COM corpus واحد skipped لغياب بوابة البيئة في التشغيل العام | GO للكود؛ تشغيل COM المخصص يبقى بوابة بيئية |

## العيب الدقيق المكتشف والمغلق

كشفت `tsc -b` أن CLI محاذاة Word/PDF يضع `wordPageMap: undefined` داخل
`AlignmentReport` رغم `exactOptionalPropertyTypes`. صار الحقل يُغفل عند غياب
الخريطة، وتُضاف خصائص fingerprint/totalPages فقط عند وجود قيم صحيحة. هذا يحفظ
JSON مطابقًا للعقد بدل قيمة اختيارية زائفة.

بعد الإصلاح:

- print-aligner + model OMML + DOM MathML: 4 ملفات / 75 اختبارًا — pass.
- TypeScript project build الكامل — pass.
- بوابة المكتبة المنشورة — pass (23 عملًا، 210,679,842 بايتًا عبر الجذور).
- بوابة Word المنشور — pass (6 أعمال، 1989 صف خريطة).

## التناقضات التوثيقية المصححة

- سجل `cantSplit` القديم حُوّل من «فجوة» إلى عقد منفذ ومختبر.
- سطر OMML التاريخي الذي قال إن الاختبارات لم تعمل وُسم بأنه أغلق لاحقًا؛ لا
  يُقرأ بعد الآن كحالة حالية.
- خطة المرحلة 2 تميز الآن قبول إصدار Alpha الحالي عن إغلاق المطابقة الشاملة.

لم تُعدل أصول الكتب أو بيانات المستخدم، ولم يحدث deploy.

---

## المصدر: word-open-performance-2026-08-09.md

# أداء فتح Word والذاكرة — 2026-08-09

## العينتان

- أكبر DOCX منشور: «الحاسم»، 650,863 بايت، 2,736 فقرة، 357 ورقة موثقة.
- corpus المعقد «زهر الخمائل»: 14,172,192 بايت، 2,524 فقرة، و22 وسيطًا.

قياس Node الحالي بعد تشغيل GC قبل/بعد الاستخراج:

| العينة | زمن استخراج النموذج | heap delta | RSS delta |
|---|---:|---:|---:|
| الحاسم | 680ms | 12MiB | 198MiB |
| زهر الخمائل | 1,048ms | 4MiB | 59MiB |

الأزمنة والذاكرة **telemetry لهذا الجهاز وليست عتبة اختبار**؛ RSS للعينة الأولى
يتضمن تحميل المحرك ووحداته، لذلك لا يصلح لمقارنة الكتابين مباشرة. العقد الحتمي
هو ترتيب المسار وحدوده، لا رقم wall-clock المتأثر بالجهاز.

## العيب المثبت والمغلق

كان `firstPageCache` يحتفظ بنسخة DOM كاملة من أول ورقة لكل كتاب فُتح طوال
عمر التبويب، بلا سقف. هذا نمو ذاكرة غير محدود ومثبت، ولا يحتاج تخمينًا بصريًا.

صار cache من نوع LRU محدودًا إلى **8 كتب**: إعادة فتح الكتب الحديثة تبقى
فورية، وإدخال التاسع يسقط الأقدم. لا تتغير الصفحة أو خطوطها أو صورها.

## العقود الحتمية

- أول صفحة Word تُبنى وتُرسم (`nextPaint`) قبل بدء `renderBookToPages` الكامل.
- عند إعادة الفتح تستعمل نسخة آمنة من أول صفحة، وفشل clone يتحول إلى cache miss.
- نافذة القارئ لا تركب أكثر من المركز ±4، وتحرر observers/cleanup عند الإزالة.
- cache نموذج البناء يحتفظ بنموذج واحد فقط حتى 32MiB، ويعيد استعماله لـ
  reader/toc/search حسب SHA-256؛ المصدر الأكبر يعالج بلا احتفاظ.
- `WordPageMap` والدقة لم يتغيرا.

## البوابات

- cache/virtualization/build cache: **3 ملفات / 11 اختبارًا** — pass.
- TypeScript: model/DOM/app — pass.
- Vite: **433 وحدة** — pass.
- Word release: 6 كتب، 663 ورقة/جذر، 1989 صف خريطة — pass.
- library release: 23 عملًا، 87 مصدرًا — pass.
- لا deploy.

---

## المصدر: word-pages-artifact-final-gate-2026-08-09.md

# بوابة حزمة Pages النهائية لـWord — 2026-08-09

## النتيجة

**PASS محليًا، بلا نشر.** حزمة `alpha-publish/pages-dist` تحمل bundle القارئ
المصحح نفسه الموجود في `app/dist`، وبيان الأعمال وخرائط Word متسقة في الجذور
الثلاثة.

## الدليل

- `verify-published-library-release.mjs`: 3 جذور، 69 ربط عمل، 87 مصدرًا،
  210,679,842 بايتًا — pass.
- `verify-published-word-release.mjs`: 18 ربط Word، 6 أعمال، 663 صفحة فريدة،
  1989 صف خريطة — pass.
- Release drift gate: 294 ملفًا، 23 عملًا، 6/6 خرائط Word؛ candidate
  `576a5f707ed67b502844d29dfe7bc3f5db6d5a1b1ef6c2dd82692579f260ca92` — pass.
- بصمات manifest وخريطة إبهاج وخريطة الحاسم متطابقة بايتًا في public/dist/Pages.
- مقارنة ملفات `index-*` و`reader-*` بين `app/dist/assets` و
  `pages-dist/assets`: صفر اختلاف اسم/بصمة.

أعادت `pages-preflight` توصيفًا صحيحًا: `authenticated=false` و
`cloudflare_authentication_required`. هذه ليست مشكلة artifact؛ هي بوابة النشر
الخارجي التي لم تُفتح عمدًا لأن المهمة تمنع deploy. لم تُطلب مصادقة ولم يحدث
اتصال نشر.

---

## المصدر: word-reader-20-cycle-lifecycle-2026-08-09.md

# دورة حياة قارئ Word — 20 فتحًا وإغلاقًا

## النتيجة

**PASS بعد إغلاق عيبين مثبتين.** أُجريت بوابة حتمية لعشرين دورة قارئ كبير ↔
مكتبة، تشمل المعاينة، observers، animation frames، listeners، object URLs،
الخطوط، والمهام الخلفية.

## العيوب المغلقة

1. أصبح cache المعاينة صنف LRU حقيقيًا: `get` ينعش ترتيب الكتاب، والسعة 8.
   الاختبار يفتح 20 كتابًا كبيرًا ويثبت بقاء 8 فقط وإسقاط الأقدم، ثم يثبت أن
   إعادة فتح كتاب تحميه من الإقصاء التالي.
2. روابط صور Word من نوع `blob:` كانت تُنشأ في `ImageCache` من غير مالك
   يحررها. صار لكل render cleanup قابل لنقل الملكية؛ يربطه القارئ بنطاق route
   ويستدعي `URL.revokeObjectURL` مرة واحدة عند الخروج. لا تُخزن معاينة ذات
   blob في cache كي لا تعاد لاحقًا بصورة مكسورة.

## الدليل الحتمي

- 20 `createObjectURL` تقابلها 20 `revokeObjectURL`.
- كل observer في الدورات يفصل مرة واحدة فقط.
- counters تعود إلى baseline بعد كل السلسلة؛ لا listener/rAF/job حي.
- تسجيل الخط الدائم المقصود يبقى 1 بعد warm-up ولا يتضاعف مع reopen.
- معاينات الصفحات ≤8، والنافذة المرئية تبقى الصفحة الحالية ±4.
- تحرير روابط صور الغلاف idempotent: الاستدعاء الثاني لا يلغي رابطًا مرتين.

## البوابات

- دورة الحياة/DOM/cache/virtualization: **6 ملفات / 151 اختبارًا** — pass.
- TypeScript: model/DOM/app — pass.
- Vite: **434 وحدة** — pass.
- Word: 6 كتب، 663 ورقة لكل جذر، 1989 صف خريطة — pass.
- المكتبة: 23 عملًا، 87 مصدرًا — pass.
- لا deploy.

---

## المصدر: published-word-all-pages-scan-2026-08-09.md

# مسح آلي لكل صفحات Word المنشورة — 2026-08-09

## النتيجة

**PASS: 6 كتب / 663 ورقة موثقة.** لا ادعاء بمطابقة بصرية بشرية لكل صفحة.

فحص المسح لكل كتاب:

- مطابقة WordPageMap الرسمية وبناء جميع مجموعات الصفحات وDOM.
- ملكية كل فقرة مرة واحدة، وتسلسل physical labels، وملكية bookmarks.
- أول/آخر نص Word أو وجود النص المرجعي داخل الصفحة حين يختلف عداد الفقرات
  البنيوية بين Word COM وOOXML.
- حل كل مرجع رأس/تذييل إلى جزء موجود.
- توقيع كل صورة/وسيط، وعدم إدخال U+FFFD، وعدم توليد NaN/Infinity أو قيم CSS
  غير معرفة.

## العيوب المكتشفة والمغلقة

1. **علاقات إبهاج:** sanitizer أعاد `<Relationship>` إلى
   `<ns0:Relationship>` وباقتباس مفرد. regex القديم لم يقرأها، فبقيت rId بدل
   `header1.xml/footer*.xml`. صار محلل OPC مستقلًا عن البادئة والاقتباس، واختبار
   corpus يثبت حل رأسي المقطعين وأربعة تذييلات.
2. **محاذاة أربع خرائط:** Word COM يضيف U+000E، يحدث أرقام TOC، ويفسر خطوط
   dingbat حسب charset؛ وفهرس قديم يعكس 73.0 في RTL. كانت المقارنة النصية
   الصارمة ترفض الخرائط رغم تطابق التسلسل والملكية. أضيفت مطابقات مقيدة بهذه
   الأدلة فقط، مع اختبار يثبت أن نصًا مؤلفًا مختلفًا لا يزال مرفوضًا.
3. **دلالة الحافة الفارغة:** firstText الفارغ يعني فقرة حافة بلا نص، لا صفحة
   فارغة. كما قد توجد فقرة OOXML بنيوية قبل أول فقرة يعدها Word COM؛ المدقق
   يطلب النص المرجعي داخل الصفحة نفسها بدل إسقاط الخريطة بسبب ترتيب حافة شكلي.

## البوابات

- model + DOM alignment + all-pages scan: **3 ملفات / 92 اختبارًا** — pass.
- TypeScript: ooxml-model/ooxml-dom/app — pass.
- بوابة الأعمال الـ23: 3 جذور / 87 مصدرًا / 210,679,842 بايتًا — pass.
- بوابة Word: 3 جذور / 18 ربطًا / 1989 صف خريطة — pass.

الحد الصادق: Fake DOM لا يقيس `scrollHeight/getBoundingClientRect` ولا يشكل
الغليفات بخط الجهاز؛ لذلك لا يحول PASS إلى ادعاء «لا clipping بصري» لكل 663
صفحة. المقارنة البصرية الشاملة تبقى NO-GO لإغلاق المرحلة 2، لا لإصدار Alpha.

---

## المصدر: published-word-high-risk-pages-2026-08-09.md

# تدقيق صفحات Word عالية المخاطر — 2026-08-09

## النتيجة

**PASS بنيوي آلي: 6 كتب / 663 ورقة.** أضيف عقد دائم يصنف الصفحات
عالية المخاطر من النموذج نفسه، ثم يمررها ضمن بوابة كل الصفحات وخرائط
`WordPageMap` الرسمية. لم يظهر عيب جديد دقيق يصلح للتعديل، لذلك لم يحدث
تغيير تخميني في محرك العرض.

## التغطية المثبتة

| الكتاب | غلاف بصورة | صفحات صور | صفحات جداول | صفحات حواشٍ | صفحات رأس/تذييل | صفحات bookmarks |
|---|---:|---:|---:|---:|---:|---:|
| إبهاج أهل الصناعة | 1 | 3 | 0 | 13 | 22 | 0 |
| الحاسم | 0 | 0 | 7 | 214 | 356 | 73 |
| الدولة الإسلامية بين الحقيقة والوهم | 0 | 0 | 0 | 111 | 162 | 28 |
| تمجيس الجذور | 0 | 0 | 1 | 29 | 48 | 17 |
| حم لا يُنصرون | 0 | 0 | 0 | 6 | 40 | 13 |
| خالعة القلوب | 0 | 0 | 9 | 19 | 34 | 0 |

الإجمالي: **3 صفحات صور، 17 صفحة جداول، 392 صفحة حواشٍ، 662 صفحة ذات
قصة رأس/تذييل، و131 صفحة bookmarks**. الغلاف المصور الموجود في هذه العينة
مملوك للورقة الأولى من «إبهاج».

لكل ورقة يثبت الاختبار:

- نجاح الخريطة الرسمية وتسلسل أرقام الورق والرقم المعدل.
- ملكية كل فقرة مرة واحدة فقط، وملكية bookmark داخل ورقة محددة.
- حل مراجع الرأس والتذييل إلى أجزاء OOXML موجودة.
- سلامة تواقيع الصور وعدم وجود وسيط فارغ.
- عدم إنتاج `NaN` أو `Infinity` أو قيم CSS غير معرفة، وعدم إدخال حرف الاستبدال.
- بناء DOM لكل الأوراق الـ663، بما فيها فئات الصور والجداول والحواشي والهوامش.

## الحد الصادق

هذا تدقيق بنيوي آلي وليس ادعاء مطابقة بصرية بشرية أو pixel equality.
بيئة Fake DOM لا تشكل خطوط الجهاز ولا تقيس clipping/overflow المحسوب. لذلك
تبقى المقارنة البصرية البشرية صفحة بصفحة بندًا منفصلًا لإغلاق المرحلة الكاملة،
بينما هذه البوابة تمنع regressions في الملكية والخرائط والوسائط والقصص الهامشية.

## البوابة

- اختبار الصفحات المنشورة عالي المخاطر: **ملف واحد / 7 اختبارات** — pass.
- يتبع ذلك TypeScript للحزم والتطبيق، وبوابتا Word والمكتبة المنشورتان.
- لا نشر خارجي في هذه الدورة.
